# 뿌린대로거두리라 — 컨텍스트 노트

작업 중 내린 결정과 근거를 계속 누적한다. 다음 세션(사람/에이전트)이 재유도 없이 이어받기 위한 문서다.

## 0. 이번 세션의 계획 (2026-09-14, 기획 단계)

**무엇을** — 경조사(결혼식·돌잔치·장례식 등) 준돈/받은돈 장부 모바일 앱의 1단계 기획 문서 일체.
**왜** — 사용자 요청이 "기획부터 잡아줘"이며, 2단계(청첩장 생성·관리·공유)로 확장 예정이므로 1단계 데이터 모델이 확장을 막지 않는지까지 점검해야 한다.

| 단계 | 목적 | 입력 | 산출물 | 검증 기준 |
|---|---|---|---|---|
| 1 | 환경·경험 실측 | 로컬 머신, ~/projects | 아래 "기술 환경 발견" | 툴체인 버전과 기존 프로젝트 스택을 파일 근거로 확인 |
| 2 | 제품 방향 확정 | 사용자 원문, 실측 결과 | docs/01, docs/04 | 로드맵 3단계, 스택 후보 3개 이상 비교, 추천안에 근거 명시 |
| 3 | 상세 기획 위임 | 01·04 + 사실 브리핑 | docs/02, docs/03 (product-planner) | 엣지 케이스(동명이인·공동 부조·부조 형태·답례) 전부 다룸, 금액 integer, UUID 키 |
| 4 | CTO 검토 | 02·03 | 수정본 | 정체성 축(UNIQUE)·단위·2단계 확장 영향을 CTO가 직접 점검 |
| 5 | 보고 | 전체 | 최종 보고 + 메모리 갱신 | 사용자 확인 질문 6~10개, 각 질문에 이유와 선택지 |

이 세션은 비대화형이라 중간 질문이 불가하다. 따라서 합리적 기본값을 "가정"으로 표시하고 진행했으며, 사용자 확인이 필요한 항목은 docs/02 말미와 최종 보고의 "사용자 확인 필요 질문"에 모았다.

## 1. 기술 환경 발견 (2026-09-14 실측)

- 로컬 툴체인 — Node **v26.4.0**, npm 11.17, **Bun 1.3.12**, Xcode **26.6**(Swift 6.3.3), CocoaPods 1.17, Android Studio + SDK(`~/Library/Android/sdk`, 단 `ANDROID_HOME` 미설정), Ruby 4.0.6.
- 없음 — Flutter/Dart, pnpm/yarn, 전역 expo/eas CLI(npx로 대체 가능), watchman, gradle/kotlin CLI(Android Studio 내장분으로 충분).
- 사용자의 기존 프로젝트 스택(파일 근거).
  - `~/projects/321go` — iOS Swift(App Store 출시) + Android Kotlin/Compose(클로즈드 베타) + Supabase. 즉 네이티브 양쪽을 따로 만들어 본 경험이 있고, 그 유지 비용도 겪었다.
  - `~/projects/FluencC` — **Expo SDK 57 / RN 0.86** 앱. `~/projects/scentilique` — Expo + Supabase 모노레포(회원앱·파트너앱·관리자웹). Expo 실전 경험이 이미 두 건이다.
  - `~/projects/coco-finance` 등 — Next.js + Supabase 웹. Supabase JS SDK·RLS에 익숙하다.
- 사용자는 1인 개발자다(CTO 메모리 project_moni_android_kickoff 근거). 스택 선택의 핵심 변수는 "한 사람이 두 플랫폼을 감당할 수 있는가"다.

## 2. 확정한 결정과 근거

(작업 진행에 따라 아래에 누적)

### 2.1 제품 방향 (docs/01)
- **원장의 축은 금액이 아니라 사람이다.** 가계부와의 차별점이며 앱 이름("뿌린대로 거두리라")의 약속이다. 사람별 수지(내가 준 합계 대 받은 합계)가 핵심 화면.
- 타깃 우선순위 — 1차 남의 행사에 내는 30~50대, 2차 내 행사(결혼·돌)를 치른 당사자, 3차 상주. 2·3차가 2단계 청첩장으로 이어진다.
- 경쟁 비교는 카테고리 수준으로만 썼다. 개별 앱을 실측하지 않았으므로 체크리스트 1단계에 "경쟁 앱 5개 이상 실측"을 남겼다.

### 2.2 스택 (docs/04) — 사용자 최종 확정 전
- **React Native + Expo(TypeScript) 추천.** 근거는 사용자가 이미 Expo 프로젝트 2개(FluencC, scentilique)를 만들었고, 2단계 청첩장이 결국 웹 페이지라 TypeScript 타입 공유가 가능하며, 321go에서 네이티브 두 벌 유지의 비용을 이미 겪었기 때문이다. Flutter는 미설치·미경험이라 후보에서 밀렸다.
- **1단계 로컬 우선(expo-sqlite + Drizzle), 로그인 없음.** 민감한 개인 데이터라 서버 보관을 피하고, 온보딩 마찰을 없앤다. 2단계 전환은 양방향 동기화가 아니라 **1회 업로드 후 서버 SoT 전환**으로 간다. 그래서 모든 테이블에 UUID PK·created_at·updated_at·deleted_at을 둔다.
- 백업 = OS 자동 백업(iOS Documents는 iCloud 백업 기본 포함, Android 자동 백업 25MB) + JSON 내보내기/가져오기.
- 모노레포·전역 상태 라이브러리·UI 프레임워크는 1단계에서 만들지 않는다. 필요가 증명될 때 도입.
- 패키지 매니저 npm(pnpm/yarn 없음). 도메인 로직은 `node --test`로 빌드 없이 테스트(scentilique 전례).

### 2.3 데이터 모델 (docs/03)
- 테이블 3개 `people`·`events`·`entries`. 열거값(관계 그룹·행사 종류·부조 형태)은 코드 상수, 테이블로 만들지 않는다.
- **CTO 검토로 바꾼 것 2건.**
  1. `entries.direction` 제거. planner 초안은 조인을 피하려고 방향을 비정규화했으나 `events.is_mine`에서 100% 파생되는 값이라 정체성 축 불일치(행사 토글, 기록의 행사 이동 시 어긋남)를 만든다. 로컬 SQLite에서 조인 비용은 무시할 수준. 대신 "기록이 있는 행사는 `is_mine` 변경 불가" 규칙을 둔다.
  2. `events.host_person_id` 추가. 초안은 남의 행사와 당사자를 제목 문자열로만 연결해, 미리 등록한 예정 행사(기록 0건)가 빠른 기록의 "기존 행사에 추가?" 판정에 안 잡혀 중복 행사가 생겼다.
- 사람을 참조하는 축은 세 곳(`entries.person_id`, `entries.co_person_id`, `events.host_person_id`). 병합·삭제는 `reassignPerson` 한 함수로 세 곳을 함께 처리한다.
- **금액 단위 규칙** — 앱 내부·DB·JSON은 언제나 원 단위 정수. "10만" 프리셋과 만원 단위 토글의 ×10,000 변환은 입력 컴포넌트 안에서만. (CTO 메모리의 100배 오류 전례 때문에 명문화)
- planner 확인 요청 7건은 전부 기본안 승인 — 공동 부조 `co_person_id` 1명, 미확정 금액 NULL, 측 2개 고정, 사람 삭제 시 병합 제안, 대리 부조는 메모, tombstone 포함 내보내기, `name_normalized` 컬럼.
- 2단계 영향 — 1단계 스키마 변경 없이 `user_id` 추가와 `invitations`·`rsvps`·`bank_accounts` 신설로 끝난다. `contact_id`만 기기 종속이라 업로드 제외.

### 2.4 PRD 범위 (docs/02)
- P0 — 이벤트 등록, 준돈 빠른 기록(탭 5회·15초 목표), 받은돈 연속 입력(1명 8초 목표), 사람 원장·수지, 이름 검색, JSON 백업.
- P1 — 필터, 통계 분해, 답례 체크, 연락처 피커(1명 가져오기만, 자동 매칭 없음), 날짜 정밀도.
- P2(첫 출시 제외) — 로컬 알림(홈 "다가오는 행사"로 대체), 앱 잠금(사용자 확인).
- 온보딩 화면 없음. 빈 상태 문구로 대체.

## 3. 가정 (사용자 답변으로 확정 필요)
- iOS·Android 동시 출시.
- Apple Developer·Google Play 개발자 계정 보유(321go 출시 근거). Play 클로즈드 테스트 요건은 이미 통과했을 가능성이 높음.
- 부부 공동 관리는 1단계 필수가 아님(로컬 우선 선택의 전제).
- 옮겨 넣을 기존 기록은 수십 건 규모(CSV 가져오기 불필요).
- 관계 그룹은 고정 6개로 충분.

## 4. 미결 사항
- docs/02 §8 사용자 확인 질문 10개. 특히 1~3번(스택·저장 방식·플랫폼)이 정해져야 체크리스트 2단계 착수 가능.
- 앱 표시명·번들 ID·아이콘 미정.
- 경쟁 앱 실측 미완.
- git 미초기화. 커밋은 사용자 요청 시.

## 5. 이번 세션의 프로세스 메모
- 비대화형 세션이라 `plan-before-research` 규칙(계획+질문만 내고 대기)을 "계획을 먼저 적고, 가정을 표시하며 진행, 질문은 말미에 모음"으로 변형 적용했다.
- product-planner는 general-purpose 에이전트에 역할 파일을 읽히는 방식으로 동기 호출했다. 산출물 품질은 높았고, CTO 검토에서 잡힌 결함은 둘 다 "정체성 축" 유형(파생 컬럼 중복, 구조 링크 누락)이었다.
