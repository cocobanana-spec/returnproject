# 뿌린대로거두리라 — 컨텍스트 노트

작업 중 내린 결정과 근거를 계속 누적한다. 다음 세션(사람/에이전트)이 재유도 없이 이어받기 위한 문서다.

## 0. 확정된 제품 결정 (2026-09-15 사용자 답변)

| 항목 | 결정 | 비고 |
|---|---|---|
| 모바일 프레임워크 | **React Native + Expo(TypeScript)** | CTO 추천안 그대로 |
| 저장 방식 | **클라우드** (서버가 처음부터 SoT) | CTO 추천안은 "로컬 우선"이었다. 사용자가 이유는 밝히지 않았다. 영향과 트레이드오프는 §2.2 |
| 출시 플랫폼 | **iOS · Android 동시** | CTO 추천안 그대로 |

아직 답변 대기인 항목은 docs/02 §8.2(4~10번)와 §8.3(11~14번, 클라우드 확정으로 새로 생김)에 있다. 착수 전 최소 필수는 14번(Supabase 확정)과 11번(공동 장부 필요 여부)이다.

## 1. 이번 세션들의 계획

### 1.1 2026-09-14 기획 세션

**무엇을** — 경조사(결혼식·돌잔치·장례식 등) 준돈/받은돈 장부 모바일 앱의 1단계 기획 문서 일체.
**왜** — 사용자 요청이 "기획부터 잡아줘"이며, 2단계(청첩장 생성·관리·공유)로 확장 예정이므로 1단계 데이터 모델이 확장을 막지 않는지까지 점검해야 한다.

| 단계 | 목적 | 입력 | 산출물 | 검증 기준 |
|---|---|---|---|---|
| 1 | 환경·경험 실측 | 로컬 머신, ~/projects | 아래 "기술 환경 발견" | 툴체인 버전과 기존 프로젝트 스택을 파일 근거로 확인 |
| 2 | 제품 방향 확정 | 사용자 원문, 실측 결과 | docs/01, docs/04 | 로드맵 3단계, 스택 후보 3개 이상 비교, 추천안에 근거 명시 |
| 3 | 상세 기획 위임 | 01·04 + 사실 브리핑 | docs/02, docs/03 (product-planner) | 엣지 케이스(동명이인·공동 부조·부조 형태·답례) 전부 다룸, 금액 integer, UUID 키 |
| 4 | CTO 검토 | 02·03 | 수정본 | 정체성 축(UNIQUE)·단위·2단계 확장 영향을 CTO가 직접 점검 |
| 5 | 보고 | 전체 | 최종 보고 + 메모리 갱신 | 사용자 확인 질문 6~10개, 각 질문에 이유와 선택지 |

비대화형 세션이라 합리적 기본값을 "가정"으로 표시하고 진행했다.

### 1.2 2026-09-15 개정 세션

사용자가 질문 1~3에 답했고 저장 방식이 추천안과 달리 "클라우드"로 확정됐다. CTO가 영향 범위를 정리한 뒤 docs/01~04, checklist.md, 이 문서를 직접 개정했다(범위가 명세돼 있어 위임보다 직접 수정이 빨랐다). 커밋은 하지 않았다(사용자가 한다).

## 2. 기술 환경 발견 (2026-09-14 실측)

- 로컬 툴체인 — Node **v26.4.0**, npm 11.17, **Bun 1.3.12**, Xcode **26.6**(Swift 6.3.3), CocoaPods 1.17, Android Studio + SDK(`~/Library/Android/sdk`, 단 `ANDROID_HOME` 미설정), Ruby 4.0.6.
- 없음 — Flutter/Dart, pnpm/yarn, 전역 expo/eas CLI(npx로 대체 가능), watchman, gradle/kotlin CLI(Android Studio 내장분으로 충분). Supabase CLI 설치 여부는 미확인(착수 시 확인).
- 사용자의 기존 프로젝트 스택(파일 근거).
  - `~/projects/321go` — iOS Swift(App Store 출시) + Android Kotlin/Compose(클로즈드 베타) + Supabase. 네이티브 양쪽을 따로 만들어 본 경험이 있고 그 유지 비용도 겪었다.
  - `~/projects/FluencC` — **Expo SDK 57 / RN 0.86** 앱. `~/projects/scentilique` — Expo + Supabase 모노레포(회원앱·파트너앱·관리자웹, Auth·RLS·Edge Function 사용). Expo 실전 경험 두 건, Supabase Auth 경험 있음.
  - `~/projects/coco-finance` 등 — Next.js + Supabase 웹. Supabase JS SDK·RLS에 익숙하다.
- 사용자는 1인 개발자다(CTO 메모리 project_moni_android_kickoff 근거). 스택 선택의 핵심 변수는 "한 사람이 두 플랫폼을 감당할 수 있는가"다.
- git — 2026-09-15 기준 `main`에 기획 문서 첫 커밋(99a787f), 원격 https://github.com/cocobanana-spec/returnproject.git.

## 3. 결정과 근거

### 3.1 제품 방향 (docs/01)
- **원장의 축은 금액이 아니라 사람이다.** 가계부와의 차별점이며 앱 이름("뿌린대로 거두리라")의 약속이다. 사람별 수지(내가 준 합계 대 받은 합계)가 핵심 화면.
- 타깃 우선순위 — 1차 남의 행사에 내는 30~50대, 2차 내 행사(결혼·돌)를 치른 당사자, 3차 상주. 2·3차가 2단계 청첩장으로 이어진다.
- 경쟁 비교는 카테고리 수준으로만 썼다. 개별 앱을 실측하지 않았으므로 체크리스트에 "경쟁 앱 5개 이상 실측"을 남겼다.

### 3.2 스택 (docs/04)
- **Expo 확정(사용자).** 근거는 Expo 프로젝트 2개 경험, 2단계 청첩장 웹과 TypeScript 타입 공유, 321go에서 겪은 네이티브 두 벌 유지 비용.
- **저장 방식 클라우드 확정(사용자, 2026-09-15).** CTO의 원래 추천은 "1단계 로컬 SQLite, 2단계에 1회 업로드"였다. 클라우드로 바뀌면서 생긴 변화와 트레이드오프는 다음과 같다.
  - 얻는 것 — 기기 변경·분실 시 로그인만으로 복원. 2단계로 가는 "로컬 → 서버 1회 업로드 마이그레이션"이 통째로 사라진다(가장 큰 이득). 소프트 삭제·tombstone·JSON 가져오기·부분 인덱스가 전부 불필요해져 데이터 모델이 단순해진다. 집계를 서버 뷰·함수로 옮겨 클라이언트 코드가 줄어든다.
  - 잃는 것 — 첫 실행에 로그인이 필요하다(온보딩 마찰 1회). 서버에 민감 데이터가 놓이므로 개인정보 처리방침 기재 항목이 늘고 계정 삭제 기능이 필수가 된다(App Store 요건). 오프라인 쓰기가 안 된다(식장·지하철 리스크). 운영 비용(Supabase Pro)이 생긴다.
  - 하지 않기로 한 것 — 오프라인 쓰기 큐, 로컬 DB, 동기화 엔진, 실시간 구독, 충돌 해결. 오프라인은 "읽기 캐시 + 쓰기 실패 시 폼 유지·수동 재시도"로 한정한다(코디네이터 지시, 단순함 우선).
- **백엔드는 Supabase 추천(사용자 확인 대기, 질문 14).** 사용자 경험 3건, 장부 집계에 SQL이 유리, 2단계 청첩장이 같은 프로젝트에 테이블 3개 추가로 끝난다. Firebase는 집계·조인에 불리하고 경험이 없다. 자체 API는 API 계층을 새로 쓰는 비용이 든다.
- **인증은 Apple + Google 기본, Kakao는 질문 12.** 첫 실행 방식은 "로그인 먼저"가 기본 가정(질문 13). 익명 시작은 계정 연결 전 앱 삭제 시 데이터가 사라져 클라우드 목적과 충돌한다.
- 스키마는 Supabase CLI 마이그레이션 SQL + `supabase gen types`. Drizzle·ORM은 쓰지 않는다. 손으로 쓴 타입도 두지 않는다(scentilique에서 교체 작업이 남았던 전례).
- 모노레포·전역 상태 라이브러리·UI 프레임워크는 1단계에서 만들지 않는다. 패키지 매니저 npm. 도메인 로직은 `node --test`.

### 3.3 데이터 모델 (docs/03)
- 테이블 3개 `people`·`events`·`entries`, 모두 `user_id` 소유 축 + RLS 정책 4개(SELECT/INSERT/UPDATE/DELETE). UPDATE 정책 누락은 오류 없이 0건 처리되므로(CTO 메모리 전례) 4개를 빠짐없이 둔다.
- **CTO 검토로 바꾼 것(2026-09-14).**
  1. `entries.direction` 제거. `events.is_mine`에서 100% 파생되는 값이라 정체성 축 불일치를 만든다. "기록이 있는 행사는 `is_mine` 변경 불가"를 트리거로 강제.
  2. `events.host_person_id` 추가. 남의 행사와 당사자를 제목 문자열로만 연결하면 예정 행사(기록 0건)가 "기존 행사에 추가?" 판정에서 빠져 중복이 생긴다.
- **클라우드 개정으로 바꾼 것(2026-09-15).** 소프트 삭제 제거(물리 삭제 + FK CASCADE/SET NULL), `name_normalized`를 generated column으로, 집계는 뷰 `person_balances` + RPC `event_summary`·`stats_by_year`, 사람 삭제·병합은 RPC `delete_person`·`merge_people`(security invoker)로 원자 처리, `contact_id` 제거(기기 종속), 1회 업로드 방침·JSON 가져오기 삭제.
- 사람을 참조하는 축은 세 곳(`entries.person_id`, `entries.co_person_id`, `events.host_person_id`). 병합 후 앱은 `entry_count`로 결과를 검증한다(RLS 0건 처리 대비).
- **금액 단위 규칙** — 앱 내부·DB·JSON은 언제나 원 단위 정수. "10만" 프리셋과 만원 단위 토글의 ×10,000 변환은 입력 컴포넌트 안에서만.
- 관례 처리 유지 — 공동 부조 `co_person_id` 1명, 미확정 금액 NULL, 측 2개 고정, 대리 부조는 메모, 단체는 `people.kind`, 날짜 불명은 `date_precision`.
- 2단계 영향 — 1단계 테이블 변경 없이 `invitations`·`rsvps`·`bank_accounts` 추가와 첫 `anon` 정책으로 끝난다.
- **가족 공동 장부 경고** — 소유 축을 `user_id`에서 `ledger_id`로 넓히면 RLS 정책 12개와 RPC 3개를 전부 다시 써야 한다. 1단계에 `ledger_id`를 미리 넣는 것은 추측성 설계라 하지 않되, 질문 11의 답이 "예"면 착수 전에 재설계한다.

### 3.4 PRD 범위 (docs/02)
- P0 — 계정(로그인·로그아웃·계정 삭제), 이벤트 등록, 준돈 빠른 기록(탭 5회·15초 목표), 받은돈 연속 입력(1명 8초 목표), 사람 원장·수지, 이름 검색.
- P1 — 필터, 통계 분해, 답례 체크, 연락처 피커(1명 가져오기만), 날짜 정밀도, JSON 내보내기.
- P2(첫 출시 제외) — 로컬 알림, 앱 잠금(질문 4).
- 화면 — S00 로그인, S16 계정 추가. S14는 백업에서 데이터 내보내기로 축소. 로그인 외 온보딩 화면 없음.
- 새 엣지 케이스 — 오프라인 저장 실패, 세션 만료, 같은 계정 두 기기(마지막 쓰기 승), Apple 이메일 숨기기, 계정 삭제.

## 4. 가정 (사용자 답변으로 확정 필요)
- Apple Developer·Google Play 개발자 계정 보유(321go 출시 근거). Play 클로즈드 테스트 요건은 이미 통과했을 가능성이 높음.
- 부부 공동 관리는 1단계 필수가 아님(질문 11). 아니라는 전제로 소유 축을 `user_id`로 설계했다.
- 첫 실행은 로그인 먼저(질문 13).
- 로그인 수단은 Apple + Google(질문 12).
- 옮겨 넣을 기존 기록은 수십 건 규모(질문 8).
- 관계 그룹은 고정 6개로 충분(질문 6).
- Supabase 무료 플랜의 비활성 일시정지 조건과 Pro 가격, EAS 무료 빌드 한도, Play 클로즈드 테스트 인원 수치는 현행 기준을 착수 시 다시 확인한다.

## 5. 미결 사항
- docs/02 §8.2·8.3 질문 11개(4~14번). 착수 전 최소 필수는 14번(Supabase)과 11번(공동 장부).
- 앱 표시명·번들 ID·아이콘 미정.
- 경쟁 앱 실측 미완.
- 2026-09-15 개정분은 미커밋(사용자가 커밋).

## 6. 프로세스 메모
- 2026-09-14 — 비대화형 세션이라 `plan-before-research` 규칙(계획+질문만 내고 대기)을 "계획을 먼저 적고, 가정을 표시하며 진행, 질문은 말미에 모음"으로 변형 적용했다. product-planner는 general-purpose 에이전트에 역할 파일을 읽히는 방식으로 동기 호출했다. 산출물 품질은 높았고, CTO 검토에서 잡힌 결함은 둘 다 "정체성 축" 유형(파생 컬럼 중복, 구조 링크 누락)이었다.
- 2026-09-15 — 저장 방식 변경은 문서 6개에 걸쳤지만 범위가 명세돼 있어 위임 없이 CTO가 직접 개정했다. 로컬→클라우드 전환은 "복잡해지는" 변경이 아니라 소프트 삭제·업로드 마이그레이션·가져오기가 사라지는 "단순해지는" 변경이었다는 점을 기록해 둔다.
