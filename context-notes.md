# 뿌린대로거두리라 — 컨텍스트 노트

작업 중 내린 결정과 근거를 계속 누적한다. 다음 세션(사람/에이전트)이 재유도 없이 이어받기 위한 문서다.

## 0. 확정된 제품 결정 (사용자 답변)

| # | 항목 | 결정 | 날짜 | 비고 |
|---|---|---|---|---|
| 1 | 모바일 프레임워크 | **React Native + Expo(TypeScript)** | 09-15 | CTO 추천안 그대로 |
| 2 | 저장 방식 | **클라우드** (서버가 처음부터 SoT) | 09-15 | CTO 추천안은 "로컬 우선". 영향은 §3.2 |
| 3 | 출시 플랫폼 | **iOS · Android 동시** | 09-15 | CTO 추천안 그대로 |
| 11 | 부부(가족) 공동 장부 | **예, 1단계에 필요** | 09-16 | CTO 추천안은 "아니오". 소유 축을 장부로 재설계, §3.5 |
| 12 | 로그인 수단 | ~~Apple + Google + Kakao~~ → **Apple + Google + 메일·비밀번호** | 09-16 → **09-24 변경** | Kakao 제외. 조사 결과는 docs/04 §3.1에 보존. 변경 내용은 §12 |
| 13 | 첫 실행 | **로그인 먼저** (익명 시작 없음) | 09-16 | CTO 추천안 그대로 |
| 14 | 백엔드 | **Supabase 확정** | 09-16 | CTO 추천안 그대로 |

4~10번은 2026-09-21에 추천안 일괄 수락으로 확정됐다(docs/02 §8.2, 뒤집기 비용 포함).

## 1. 세션 이력

### 1.1 2026-09-14 기획 세션

**무엇을** — 경조사 준돈/받은돈 장부 모바일 앱의 1단계 기획 문서 일체. **왜** — "기획부터 잡아줘". 2단계(청첩장) 확장을 1단계 데이터 모델이 막지 않는지까지 점검.

| 단계 | 목적 | 산출물 | 검증 기준 |
|---|---|---|---|
| 1 | 환경·경험 실측 | §2 | 툴체인 버전과 기존 프로젝트 스택을 파일 근거로 확인 |
| 2 | 제품 방향 확정 | docs/01, docs/04 | 로드맵 3단계, 스택 후보 3개 이상 비교 |
| 3 | 상세 기획 위임 | docs/02, docs/03 (product-planner) | 엣지 케이스 전부, 금액 integer, UUID 키 |
| 4 | CTO 검토 | 수정본 | 정체성 축·단위·2단계 확장 영향 직접 점검 |
| 5 | 보고 | 최종 보고 + 메모리 | 확인 질문 6~10개, 이유와 선택지 |

### 1.2 2026-09-15 개정 — 저장 방식 클라우드
질문 1~3 답변 반영. CTO가 영향 범위를 정리한 뒤 docs/01~04, checklist, 이 문서를 직접 개정.

### 1.3 2026-09-16 개정 — 공동 장부·로그인 3종·Supabase 확정
질문 11~14 답변 반영. 11번이 소유 축을 바꾸는 변경이라 docs/03을 전면 재작성하고, Kakao 로그인은 Supabase 공식 문서를 조회해 사실을 확인한 뒤 docs/04에 반영. 위임 없이 CTO가 직접 개정. 커밋은 하지 않았다(사용자가 한다).

## 2. 기술 환경 발견 (2026-09-14 실측)

- 로컬 툴체인 — Node **v26.4.0**, npm 11.17, **Bun 1.3.12**, Xcode **26.6**(Swift 6.3.3), CocoaPods 1.17, Android Studio + SDK(`~/Library/Android/sdk`, 단 `ANDROID_HOME` 미설정), Ruby 4.0.6.
- 없음 — Flutter/Dart, pnpm/yarn, 전역 expo/eas CLI(npx로 대체 가능), watchman, gradle/kotlin CLI. Supabase CLI 설치 여부는 미확인(착수 시 확인).
- 사용자의 기존 프로젝트 스택(파일 근거).
  - `~/projects/321go` — iOS Swift(App Store 출시) + Android Kotlin/Compose(클로즈드 베타) + Supabase. 네이티브 양쪽을 따로 만들어 본 경험이 있고 그 유지 비용도 겪었다.
  - `~/projects/FluencC` — **Expo SDK 57 / RN 0.86** 앱. `~/projects/scentilique` — Expo + Supabase 모노레포(Auth·RLS·Edge Function 사용).
  - `~/projects/coco-finance` 등 — Next.js + Supabase 웹.
- 사용자는 1인 개발자다(CTO 메모리 project_moni_android_kickoff 근거).
- git — `main`에 99a787f(기획 문서), 1ccfa58(클라우드 개정) 푸시됨. 원격 https://github.com/cocobanana-spec/returnproject.git. 2026-09-16 개정분은 미커밋.

## 3. 결정과 근거

### 3.1 제품 방향 (docs/01)
- **원장의 축은 금액이 아니라 사람이다.** 가계부와의 차별점이며 앱 이름의 약속이다. 사람별 수지가 핵심 화면.
- 장부는 "우리 집"의 원장이다. 공동 장부 확정으로 완료 기준도 "사용자 부부가 한 장부에" 기록을 옮기는 것으로 바뀌었다.
- 타깃 우선순위 — 1차 남의 행사에 내는 30~50대, 2차 내 행사 당사자, 3차 상주.
- 경쟁 비교는 카테고리 수준. 실측은 체크리스트에.

### 3.2 스택 (docs/04)
- **Expo 확정.** Expo 프로젝트 2개 경험, 2단계 웹과 TypeScript 타입 공유, 네이티브 두 벌 유지 비용 회피.
- **클라우드 확정(09-15).** 원래 추천은 "로컬 우선 + 2단계 1회 업로드"였다. 얻는 것 — 기기 변경 시 로그인만으로 복원, 업로드 마이그레이션 소멸, 소프트 삭제·tombstone·가져오기 소멸로 모델 단순화. 잃는 것 — 첫 실행 로그인, 처리방침 항목 증가, 계정 삭제 필수, 오프라인 쓰기 불가, 운영 비용. 하지 않기로 한 것 — 오프라인 쓰기 큐, 로컬 DB, 동기화, 실시간, 충돌 해결.
- **Supabase 확정(09-16).** 사용자 경험 3건, SQL 집계, RLS 헬퍼 하나로 공동 장부 규칙 강제, 2단계 동일 프로젝트.
- 스키마는 Supabase CLI 마이그레이션 SQL + `supabase gen types`. ORM 없음. 모노레포·전역 상태 라이브러리·UI 프레임워크는 1단계에 없음. npm. `node --test`.

### 3.3 데이터 모델 — 09-14·09-15 결정 (유지)
- `entries.direction` 없음(`events.is_mine`에서 파생), `events.host_person_id`(남의 행사 당사자), 공동 부조 `co_person_id` 1명, 미확정 금액 NULL, 측 2개 고정, 대리 부조는 메모, 단체는 `people.kind`, 날짜 불명은 `date_precision`.
- 물리 삭제 + FK 규칙, `name_normalized` generated column, 집계는 뷰 + RPC, 사람 삭제·병합은 RPC. 오프라인은 읽기 캐시만.
- **금액 단위 규칙** — 원 단위 정수만. 만원 토글 변환은 입력 컴포넌트 안에서만.

### 3.4 PRD 범위 (docs/02)
- P0 — 계정(로그인 3종·계정 삭제), **장부 공유**, 이벤트 등록, 준돈 빠른 기록, 받은돈 연속 입력, 사람 원장·수지, 이름 검색.
- P1 — 필터, 통계 분해, 답례 체크, 연락처 피커, 날짜 정밀도, JSON 내보내기.
- P2 — 로컬 알림, 앱 잠금(질문 4).
- 화면 — S00 로그인, S16 계정, **S17 장부**. 시나리오 C(초대·합류).

### 3.5 공동 장부 재설계 (09-16, docs/03) — 결정과 근거
- **소유 축 = 장부.** `ledgers` + `ledger_members(ledger_id, user_id, role, display_name)` 추가, people·events·entries의 `user_id`를 `ledger_id`로 교체. RLS는 `is_ledger_member(ledger_id)` SECURITY DEFINER 헬퍼 하나로 통일(ledger_members 정책의 자기 참조 재귀 회피). 이유 — 부부가 한 장부를 쓰려면 행의 주인이 사람이 아니라 장부여야 한다.
- **역할 owner/member, 차이는 초대·제거만.** 부부 장부에서 데이터 권한 차이는 의미가 없다.
- **초대는 8자·24시간·1회용 코드.** 딥링크(유니버설 링크·도메인), 전화번호 매칭(수집·인증), 이메일 초대(발송 인프라)보다 서버 함수 하나 + 입력창 하나로 끝난다. 부부는 카톡으로 이어져 있어 코드 전달이 자연스럽다.
- **장부 생성은 `auth.users` 트리거만.** "장부 만들기" UI 없음. 모든 사용자가 장부 1권을 보장받고 클라이언트 코드가 필요 없다.
- **여러 장부 허용 + 전환 UI, 합치기 없음.** 합류 전 개인 장부에 기록이 있던 경우를 강제로 합치거나 버리게 하는 것보다 단순. `join_ledger`가 빈 개인 장부(유일 구성원·데이터 0건)만 자동 정리 — 주 흐름(배우자 설치 → 빈 장부 → 코드 입력)에서 쓸모없는 장부가 남는 것을 막기 위함.
- **구성원 변경은 SECURITY DEFINER RPC만**(`create_invite_code`·`join_ledger`·`remove_member`·`prepare_account_deletion`). "마지막 구성원은 못 나감", "owner 승계(가장 먼저 합류한 구성원)" 같은 규칙은 정책식보다 함수 안 IF문이 명확하다.
- **계정 삭제 = 혼자면 장부와 데이터 삭제, 아니면 구성원만 제거.** Edge Function이 사용자 JWT로 RPC를 먼저 호출한 뒤 service role로 사용자 삭제.
- **입력자는 `entries.created_by`만.** 공동 장부에서 "누가 넣었지"는 실제 질문이지만 사람·행사는 공유 자산이라 입력자가 의미 없다. 필터·통계 축으로 쓰지 않는다.
- **구성원 표시 이름은 `ledger_members.display_name`에 합류 시 복사.** `auth.users`는 클라이언트가 못 읽고, `profiles` 테이블보다 컬럼 하나가 싸다.
- **"공동 부조"(상대방 부부, `co_person_id`)와 "공동 장부"(우리 부부)는 독립 개념**으로 docs/03 §4·docs/02 §5에 명시.
- **구현 필수 조건 두 가지** — (a) 모든 조회에 `ledger_id = 현재 장부` 필터(RLS는 내 모든 장부를 허용하므로 빠지면 섞인다), (b) entries의 event_id·person_id가 같은 장부인지 검사하는 트리거(두 장부 구성원의 교차 참조 차단).
- **알려진 한계(문서화)** — 구성원에서 제거된 사용자는 장부 0권이 될 수 있고 새 개인 장부를 만들 UI가 없다(계정 삭제 후 재로그인이 유일 경로). 드문 경우라 "장부 만들기"를 추가하지 않았다. 관계 그룹 "직장"이 남편·아내 어느 쪽인지 구분하는 축이 없다(구분 라벨로 대체).
- **2단계 영향** — `invitations`·`rsvps`·`bank_accounts`도 `ledger_id` 소유. "가족 공동 장부"는 이후 후보에서 빠지고 "장부 합치기"가 들어갔다.

### 3.6 Kakao 로그인 사실 확인 (09-16, Supabase 공식 문서 auth-kakao 조회)
- 확인됨 — Kakao는 Supabase Auth **기본 제공 프로바이더**. REST API 키 = client_id, Client Secret 활성화 필요, Redirect URI에 Supabase 콜백 등록, 동의 항목 `profile_nickname`·`profile_image`·`account_email`(선택). **`account_email`은 비즈 앱에서만** 사용 가능. 문서에 `signInWithOAuth` 외에 ID 토큰을 `signInWithIdToken`으로 넘기는 경로도 기술됨.
- 검증 필요(지어내지 않음) — (a) React Native 네이티브 Kakao SDK + OIDC ID 토큰 → `signInWithIdToken({ provider: 'kakao' })`가 실제 동작하는지(JS 레퍼런스 페이지에는 프로바이더 목록이 없었다). 로컬 Supabase에서 1회 시도, 안 되면 브라우저 OAuth 경로. (b) 개인 개발자의 비즈 앱 전환 조건. (c) 이메일 없는 Kakao 사용자를 Supabase가 어떻게 만드는지.
- 설계상 이메일은 식별에 쓰지 않으므로(사용자 id 기준, 표시 이름은 닉네임) 비즈 앱 전환 없이도 기능상 문제가 없다.

## 4. 가정 (사용자 답변으로 확정 필요)
- Apple Developer·Google Play 개발자 계정 보유(321go 출시 근거). Play 클로즈드 테스트 요건은 이미 통과했을 가능성이 높음.
- 옮겨 넣을 기존 기록은 수십 건 규모(질문 8).
- 관계 그룹은 고정 6개로 충분(질문 6).
- Supabase 무료 플랜 일시정지 조건·Pro 가격, EAS 무료 빌드 한도, Play 클로즈드 테스트 인원, Kakao 개인 개발자 비즈 앱 조건은 착수 시 현행 기준을 다시 확인한다.

## 5. 미결 사항
- docs/02 §8.2 질문 4~10(착수 비차단).
- 앱 표시명·번들 ID·아이콘 미정. 경쟁 앱 실측 미완.
- 2026-09-16 개정분 미커밋(사용자가 커밋).

## 6. 프로세스 메모
- 09-14 — 비대화형이라 `plan-before-research`를 "계획 먼저, 가정 표시하며 진행, 질문은 말미"로 변형 적용. product-planner는 general-purpose에 역할 파일을 읽혀 동기 호출. CTO 검토에서 잡힌 결함은 둘 다 정체성 축 유형.
- 09-15 — 로컬→클라우드는 소프트 삭제·업로드·가져오기가 사라지는 "단순해지는" 변경이었다.
- 09-16 — 소유 축 확장은 예고한 대로 RLS 정책과 RPC를 전부 다시 쓰는 일이었다. 착수 전에 확인 질문으로 막아 둔 것이 맞았다. 외부 서비스 사실(Kakao)은 공식 문서를 직접 조회하고, 확인 안 된 부분은 "검증 필요 + 확인 방법"으로 남겼다.

## 7. 백엔드 토대 구현 (2026-09-19)

checklist 3단계를 구현했다. 산출물은 `supabase/migrations/0001_init.sql`(정본), `supabase/functions/delete-account/index.ts`, `supabase/tests/`(스텁·검증·러너).

### 7.1 검증 경로를 왜 이렇게 골랐나
- 이 머신에 **컨테이너 런타임이 전혀 없다**(docker·colima·podman·lima 모두 없음). 그래서 `supabase start`·`supabase db lint`·`gen types --local` 등 Docker 의존 명령을 쓸 수 없다. Supabase 클라우드 프로젝트도 아직 없다(checklist 2단계 미완).
- 택한 경로는 **`brew install postgresql@17`(17.11) + auth 스텁**이다. 로컬에 진짜 Postgres를 띄우고, 마이그레이션이 의존하는 Supabase 제공물만 스텁으로 만든 뒤(`auth` 스키마, `auth.users`, `auth.uid()`, 역할 anon/authenticated/service_role, `extensions` 스키마의 pgcrypto, public 스키마 기본 권한) **`0001_init.sql`을 수정 없이 그대로** 적용했다. DDL·생성 컬럼·트리거·plpgsql·RLS·컬럼 권한이 전부 실제로 실행된다.
- 대안이던 colima+docker 설치는 수 GB 다운로드라 느리고, 얻는 것은 "Supabase 이미지와 동일한 환경" 하나뿐이다. 그 차이에서 오는 위험은 checklist 3b로 명시해 관리한다.
- 스텁은 **`supabase/tests/`에 둔다. 마이그레이션 디렉터리에 절대 넣지 않는다.** 프로덕션에 섞이면 `auth` 스키마를 덮어쓴다.

### 7.2 검증 결과
- `./supabase/tests/run.sh` — **154건 전부 통과**, 실패 시 종료 코드 ≠ 0. 계정 A·B(같은 장부), C(다른 장부), D(두 장부 구성원) 네 세션으로 돌린다.
- 하네스의 핵심은 `expect_rows`다. **RLS에 걸린 UPDATE·DELETE는 오류 없이 0건 처리**되므로(CTO 메모리 전례), 모든 쓰기 검사는 영향 행 수를 직접 확인한다. 조회 검사는 관리자 자격(`expect_admin`)과 사용자 자격(`expect_scalar`)을 구분한다.

### 7.3 구현하며 고친 기획 결함
1. **남의 행사의 당사자 필수를 CHECK로 묶으면 사람 삭제가 불가능하다**(docs/03 CTO 결정 25로 기록). FK의 `ON DELETE SET NULL`이 당사자를 비우는 순간 CHECK에 걸려 삭제 자체가 실패했다(T11.5에서 실제로 터짐). docs/02 §5의 "당사자만 비운다"와 모순이었다. CHECK는 "내 행사는 당사자 없음"만 남기고, "남의 행사는 당사자 필수"는 `events_validate` 트리거가 INSERT일 때만 본다.
2. **데이터 수정 CTE는 서로의 결과를 보지 못한다**(결정 26). 사람·행사·기록을 `with ... insert ... insert` 하나로 묶으면 뒤 문장이 앞 CTE의 행을 못 봐 FK와 같은 장부 트리거가 전부 실패한다. 앱의 빠른 기록은 순차 INSERT(왕복 3회)여야 한다. docs/02 §3.2에 구현 주의로 적었다.
3. **`ledger_id`를 UPDATE로 바꿔 공유 장부의 행을 개인 장부로 빼낼 수 있었다**(QA가 잡음). RLS의 USING(옛 장부)과 WITH CHECK(새 장부)가 두 장부 모두의 구성원에게는 둘 다 참이라 정책으로는 막히지 않는다. `forbid_ledger_change` 트리거를 people·events·entries에 달았다. 기밀성 유출은 아니지만(두 장부 모두의 구성원이어야 함) 배우자가 보던 데이터가 조용히 사라지는 경로였다.
4. **`delete_person`이 무관한 예정 행사까지 지웠다**(QA가 잡음). 장부 전체의 "당사자 NULL + 기록 0건" 행사를 쓸어 담았다. "지운 사람이 당사자이던" 행사로 한정했다.
5. 검증 스크립트의 **가짜 통과 1건**(QA가 잡음). "소멸한 코드 재사용 불가" 검사가 실제로는 없는 코드를 넣고 있어, `join_ledger`의 코드 소멸 로직이 사라져도 통과했다. 실제로 소비된 코드를 넣도록 고쳤다.

### 7.4 의식적으로 하지 않은 것 (QA 지적 중)
- **`entries.created_by` 위조 방지.** 같은 장부 구성원끼리는 서로의 입력자 값을 바꿀 수 있다. 막으려면 INSERT/UPDATE 권한을 컬럼 단위로 열거해야 해서 컬럼이 늘 때마다 유지비가 든다. 읽기 권한은 어차피 동등하므로 **입력자는 표시용이며 감사 근거가 아니다**로 문서화하고 넘어간다.
- **초대 코드 동시 사용 잠금(`for update`)과 시도 횟수 제한.** 카톡으로 1명에게 보내는 40비트 코드라 현실성이 낮다.
- **Edge Function의 부분 실패 보상.** `prepare_account_deletion` 성공 후 `deleteUser`가 실패하면 데이터는 사라졌는데 계정이 남는다. RPC가 멱등이라 재호출로 복구되며, "데이터가 주인 없이 남는" 위험한 방향은 아니다.
- 앱이 다룰 몫 — 이름 trim(공백만인 이름이 CHECK를 통과한다), 측 라벨 없는 행사에 `side` 넣지 않기, `host_person_id`를 UPDATE 페이로드에 넣지 않기.

### 7.5 검증하지 못한 것
- **실제 Supabase 전부.** `db push` 권한, `auth.users` 트리거 생성 가능 여부, 호스티드의 기본 권한·pgcrypto 설치 스키마·로케일. checklist 3b로 목록화했다.
- **Edge Function 런타임.** Deno와 Supabase 런타임이 없어 코드만 작성했다. 타입 체크도 못 했다.
- **타입 생성.** `supabase gen types typescript --db-url`은 CLI 2.90.0에서도 postgres-meta 컨테이너를 띄운다. Docker 없이는 경로가 없다. 손으로 쓰지 않고 미실시로 둔다.
- **Supabase CLI 버전** — 설치본 2.90.0, 최신 2.117.0. 업데이트 권고가 뜬다.

## 8. 실제 Supabase 연결과 원격 검증 (2026-09-20)

사용자가 프로젝트 URL과 anon 키를 줬다. 프로젝트 `ekcjfqqiopajlcbqgvfo` (returnproject, 서울 리전, 2026-09-19 생성).

### 8.1 적용과 확인 방법
- `supabase link` 후 `supabase db push`로 0001·0002를 적용했다. DB 비밀번호는 필요 없었다. CLI가 키체인의 액세스 토큰으로 임시 로그인 역할을 만든다(`Initialising login role...`).
- 타입 생성은 `--project-id`를 쓰면 Docker 없이 된다. `--db-url`만 컨테이너를 띄운다. 7.5의 "경로가 없다"는 원격 연결 후 해소됐다.
- Edge Function 배포도 Docker 없이 됐다(`WARNING: Docker is not running`만 뜨고 업로드 성공).
- 검증은 `supabase/tests/remote_smoke.py`로 스크립트화했다. 진짜 계정을 만들어 REST·RPC·Edge Function을 호출하고 끝나면 전부 지운다. 27건 전부 통과, 잔여 장부 0권.
- 테스트 계정은 메일/비밀번호로 만들었다. 관리자 API에 `email_confirm: true`를 주면 확인 메일이 나가지 않는다. `example.com`은 Supabase가 거부하므로 쓸 수 없다.
- service role 키는 저장소에 두지 않는다. `supabase projects api-keys --project-ref <ref>`로 그때그때 받고, 스크립트는 환경변수로만 읽는다.

### 8.2 원격에서만 확인할 수 있던 것
- `auth.users`에 트리거를 만들 권한이 **있었다**. 3b에서 가장 큰 위험으로 잡았던 항목이 해소됐다.
- pgcrypto도 정상이다. `create_invite_code`가 실제로 8자 코드를 발급한다.
- `name_normalized`는 호스티드 로케일에서도 `' 김 철수 '` → `'김철수'`로 같다.
- anon은 테이블 5개 전부 `permission denied`다.

### 8.3 원격 검증에서 발견한 결함 — 고아 장부 (마이그레이션 0002)
정상 경로인 Edge Function은 `prepare_account_deletion`으로 장부를 먼저 정리한다. 그러나 **대시보드·관리자 API로 `auth.users`에서 계정을 바로 지우면** `ledger_members`만 CASCADE로 사라지고 장부와 사람·행사·기록은 그대로 남는다. 구성원이 없으니 RLS상 누구에게도 보이지 않고 앱으로 지울 수도 없다. 검증 중 실제로 고아 장부 2건(사람 2명 포함)이 쌓인 것을 보고 발견했다.

개인정보가 주인 없이 영구히 남는 경로라 그냥 둘 수 없다. `ledger_members` AFTER DELETE 트리거로 (1) 구성원이 하나도 안 남으면 장부를 지우고, (2) 구성원은 남았는데 owner만 사라졌으면 `ensure_owner`로 승계시킨다. (2)도 같은 경로의 결함이었다. `remove_member`와 `prepare_account_deletion`은 `ensure_owner`를 직접 부르지만 CASCADE는 아무것도 부르지 않아 owner 없는 장부가 되고, 그러면 초대 코드 발급과 구성원 제거가 영영 불가능해진다. 로컬 검증 T14.9가 이걸 잡았다.

재귀는 일어나지 않는다. 장부를 지우면 `ledger_members`가 CASCADE로 지워지며 트리거가 다시 돌지만, 그때 장부 행은 같은 트랜잭션의 앞선 명령이 이미 지운 뒤라 안쪽 DELETE가 0건으로 끝난다.

**교훈** — 정리 로직을 애플리케이션 경로(Edge Function·RPC)에만 두면 그 경로를 타지 않는 삭제에서 데이터가 고아가 된다. 소유 관계의 정리는 DB 트리거에 두어야 경로와 무관하게 성립한다.

### 8.4 검증 과정에서 내가 틀렸던 것
첫 원격 검증에서 2건이 실패했는데 구현이 아니라 검증 쿼리의 오류였다. `ledger_members`를 `user_id` 필터 없이 조회해 "내 장부 목록"으로 쓰면, 공유 장부에서는 다른 구성원의 행까지 돌아온다(SELECT 정책이 `is_ledger_member(ledger_id)`이므로 정상). docs/03 §9.2가 이미 `user_id=eq.{me}`를 쓰라고 적어 둔 대로다. 앱 리포지토리도 같은 실수를 할 수 있으니 주의한다.

### 8.5 남은 미검증
- 소셜 로그인 3종(Apple·Google·Kakao)으로 같은 트리거 경로 확인. 프로바이더 활성화 후.
- 원격 `max_rows` 기본값 1000. 수년치 기록 조회가 조용히 잘리므로 앱에서 페이지네이션이 필수다.
- `graphql_public` 노출 범위.

## 9. 앱 토대 구현 (2026-09-21)

checklist 4단계의 자바스크립트 계층을 끝까지 쌓았다. 네이티브 빌드가 불가능한 상태라 화면 동작은 확인하지 못했고, 대신 타입·단위 테스트·번들 export·실제 프로젝트 통합 검증 네 가지로 받쳤다.

### 9.1 환경 제약과 그에 맞춘 검증 수단
- **네이티브 빌드 불가.** Xcode 27.0이 설치돼 있으나 라이선스 미동의라 CocoaPods가 막히고(`sudo xcodebuild -license accept` 필요), Android는 SDK만 있고 Java 런타임이 없다. `expo run:ios`·`run:android` 둘 다 못 돌린다.
- 그래서 검증은 네 가지로 했다. `npx tsc --noEmit`(0건), `npm test`(39건), `npx expo export --platform ios`(번들 3.2MB 생성 — 네이티브 없이 JS 번들 오류를 잡는 수단), `npm run integration`(실제 프로젝트 상대 78건).
- 스택은 Expo SDK **57.0.24** / React 19.2.3 / RN 0.86.3 / TypeScript 6.0.3. FluencC와 같은 SDK 계열이다.

### 9.2 설치·설정에서 걸린 것
- `react-dom@19.3.0`이 expo-router 경유로 끌려와 `react@19.2.3`과 peer 충돌을 냈다. `package.json`의 `overrides`로 `react-dom`을 19.2.3에 고정해 풀었다. `--legacy-peer-deps`로 덮지 않았다.
- `babel.config.js`를 직접 두면 `babel-preset-expo`가 루트에 있어야 한다. 템플릿은 babel 설정을 아예 만들지 않는데, 우리는 명시적으로 두고 preset을 설치했다.
- tsconfig에 `types: ['node','react']`를 명시해야 `node:test`와 `process`가 잡혔다. `expo/tsconfig.base`에는 `types` 필드가 없다.
- 상대 import 확장자 — 도메인·lib·리포지토리는 `.ts`를 명시한다. Node 26의 타입 스트리핑이 확장자를 요구하기 때문이며(scentilique 전례), Metro도 명시 확장자를 그대로 해석한다. `.tsx`(화면·Provider)는 Node가 부를 일이 없어 확장자를 붙이지 않았다.

### 9.3 설계 결정
- **Supabase 클라이언트를 플랫폼 중립 모듈로 분리했다.** `src/lib/supabaseClient.ts`가 `createDb`/`setDb`/`db()`를 갖고, `src/lib/supabase.ts`(RN 전용)가 AsyncStorage를 꽂는다. 리포지토리는 `db()`만 쓴다. 이유는 두 가지다. (1) Node에서 리포지토리를 그대로 불러 통합 검증을 돌릴 수 있다. (2) 2단계 웹이 붙을 때 같은 리포지토리를 재사용할 수 있다.
- **소셜 로그인 3종을 브라우저 OAuth(PKCE) 하나로 구현했다.** 기획(docs/04 §3)은 Apple을 `expo-apple-authentication` + `signInWithIdToken`, Google을 `@react-native-google-signin`으로 적었다. 바꾼 이유 — 네이티브 모듈은 빌드가 돼야 검증이 되는데 지금은 빌드가 불가능하고, 클라이언트 ID도 없어 설정 자체를 못 한다. 브라우저 흐름은 세 프로바이더에 똑같이 적용되고 앱에 ID를 넣을 필요가 없다. 바꿀 자리는 `src/auth/providers.ts` 한 파일이며, **iOS 심사 전에는 Apple을 네이티브 흐름으로 되돌리는 편이 좋다**(checklist 4b).
- **번들 ID는 `com.cocobanana.ppurin`, 표시명은 `뿌린대로거두리라`로 임시 확정했다.** 사용자가 아직 정하지 않았고 콘솔 등록 전이라 지금이 바꾸기 가장 싼 시점이다. 바꾸려면 `app.json`의 `ios.bundleIdentifier`·`android.package` 두 줄이다.

### 9.4 QA가 잡은 것과 수정
- **P0 — RPC 3종이 `ledgerId`를 받고도 쓰지 않았다.** `delete_person`·`merge_people`·`event_summary`는 SECURITY INVOKER라 RLS만 탄다. RLS는 "내가 구성원인 모든 장부"를 허용하므로, 두 장부 구성원이 다른 장부의 사람을 실제로 지울 수 있었다(QA가 원격에서 재현). 리포지토리에서 호출 전에 `.eq('ledger_id')`가 걸린 조회로 소속을 확인하도록 고쳤다. **근본 수정은 서버 함수가 `p_ledger_id`를 받는 것이고 checklist 4c에 남겼다.** 방어가 아직 앱에만 있다.
- **통합 검증이 이 위험을 못 잡고 있었다.** 계정이 전부 장부 1권짜리라 리포지토리에서 장부 필터를 통째로 빼도 통과했다. 두 장부에 동시에 속한 계정(dave)을 만들어 조회 분리와 파괴적 RPC 차단을 검사하도록 했다. 이 시나리오가 P0를 잡는 유일한 검사다.
- 로그아웃이 영속 캐시를 안 비웠다(PRD §3.12 위반). `queryClient.clear()` + AsyncStorage 키 2개 제거로 고쳤고, `LedgerProvider`도 `userId`가 null이 되면 선택을 비운다.
- `listEvents`에 정렬 타이브레이커가 없어 같은 날 행사가 페이지 사이에서 겹치거나 빠질 수 있었다. `.order('id')`를 붙였다.
- 단위 테스트에 구조적으로 실패할 수 없는 동어반복 단언이 있었다(측 합계를 같은 루프의 값으로 검증). 손으로 계산한 기대값 비교로 바꿨다.
- 그 밖에 — `pageRange`가 서버 상한 1000을 넘지 않게 클램프, 검색어에 `%`·`_` 와일드카드가 섞이지 않게 정규화, 동적 import 제거, 존재하지 않는 파일을 가리키던 주석 수정.

### 9.5 확인된 사실
- **이름 정규화가 DB 생성 컬럼과 일치한다.** JS `\s`와 Postgres `[[:space:]]`가 갈릴 수 있는 NBSP(U+00A0)·전각 공백(U+3000)을 포함한 11종을 실제 프로젝트에 넣어 `name_normalized`와 대조했고 불일치 0건이다. 이 대조는 `supabase/tests/app_integration.mjs`가 매번 다시 한다.
- anon 키는 `.env.local`(gitignore)에만 있고 추적 파일에는 JWT 문자열이 없다. service role 키는 앱 코드에 없고 통합 검증이 환경변수로만 읽는다.
- 번들에는 anon 키와 프로젝트 URL이 인라인된다(설계상 정상). service role은 없다.

### 9.6 미검증
- 화면 동작 전부. 소셜 로그인 실동작, AsyncStorage 세션 영속, 쿼리 캐시 복원, 오프라인 배너, 다크 모드.
- **Hermes 런타임 의존 두 가지가 가장 위험하다.** `String.prototype.normalize`와 `Number.prototype.toLocaleString('ko-KR')`. Node에서는 되지만 Hermes는 ICU 구성에 따라 없거나 로케일을 무시할 수 있고, 각각 이름 정규화 전체와 금액 표시 전체가 걸려 있다. checklist 4b의 1·2번이다.

## 9b. 데이터 RPC의 장부 가드 (2026-09-21, 마이그레이션 0003)

앱 토대 QA가 잡아 checklist 4c로 미뤄 두었던 것을 서버로 내렸다.

`delete_person`·`merge_people`·`event_summary`는 SECURITY INVOKER라 RLS만 탄다. RLS는 "내가 구성원인 모든 장부"를 허용하므로, 두 장부의 구성원이 현재 보고 있지 않은 장부의 사람을 지우거나 합칠 수 있었다. 사용자 사이의 권한 침해는 아니다. 그 사용자는 두 장부 모두에 정당한 권한이 있다. 문제는 **화면 맥락과 무관한 장부가 조용히 바뀌는 것**이다. 앱이 잘못된 id를 넘기는 버그 하나가 다른 장부의 데이터를 지우는 경로가 된다.

세 함수가 첫 인자로 `p_ledger_id`를 받게 했다. **기본값은 주지 않았다.** 기본값이 있으면 호출부가 장부를 빼먹어도 그대로 통과해 가드가 없는 것과 같아진다(CTO 메모리의 `rpc_default_param_overload` 전례). 읽기인 `event_summary`만 예외 대신 장부 조건을 걸어 빈 결과로 끝낸다.

앱 리포지토리의 기존 사전 확인은 그대로 뒀다. 이제 중복이지만, 서버 오류 코드를 그대로 보여 주는 것보다 "이미 삭제된 사람입니다" 같은 문구를 만들기에 낫다. 서버가 최종 방어선이고 앱은 문구 생성기다.

`entries.amount` 상한 CHECK(10억)도 함께 넣었다. 앱만 막고 DB에는 상한이 없었다.

**검증** — 로컬 177건(T15 신설 14건 포함), 원격 스모크 32건(§9 신설 5건), 앱 통합 78건, 단위 39건, `tsc --noEmit` 0건. 원격 §9는 실제로 두 장부에 속한 계정을 만들어 REST로 직접 RPC를 불러 `wrong_ledger`가 나오는지 본다. 앱 리포지토리를 거치지 않으므로 서버가 스스로 막는다는 증거다.

**교훈** — 방어를 앱에만 두면 "앱을 거치지 않는 호출"이 그대로 뚫린다. 그리고 테넌트 가드를 시험하려면 **두 테넌트에 동시에 속한 계정**이 검증에 있어야 한다. 계정이 전부 단일 테넌트면 필터를 통째로 지워도 모든 검사가 통과한다.

## 10. Hermes 런타임 확인 (2026-09-21)

사용자가 Xcode 라이선스에 동의해 iOS 시뮬레이터가 열렸다. 의존성이 전부 Expo SDK 57 범위라 **네이티브 dev client 빌드 없이 Expo Go로 실행**된다. 앱 토대에서 가장 컸던 미검증 위험 2건을 여기서 털었다.

확인 방법 — 임시 진단 모듈을 `app/_layout.tsx` 맨 위에서 import해 결과를 `console.log`로 찍고 Metro 로그에서 읽었다. 확인 후 모듈과 import를 모두 지웠다(저장소에 남기지 않는다).

| 항목 | 결과 |
|---|---|
| 런타임 | Hermes, `Intl` 있음 |
| `'김철수'.normalize('NFC')` | 통과 |
| NFD로 분해한 '감'(3코드유닛) → NFC | 통과 |
| `/\s/`가 NBSP·전각 공백에 매치 | 둘 다 통과 |
| `' 김 철수 '` 정규화 | `'김철수'` (Node·DB와 동일) |
| `(1234567).toLocaleString('ko-KR')` | `1,234,567` |

즉 `src/domain/name.ts`와 `money.ts`를 고칠 필요가 없다. 셋(Node·Hermes·Postgres)이 같은 결과를 낸다.

시뮬레이터 화면도 확인했다. 로그인 화면이 뜨고 제목·설명·소셜 3종 버튼이 렌더된다. 세션이 없으므로 `(auth)` 그룹으로 라우팅된 것이며 세션 복원 경로가 동작한다는 뜻이다. 다만 화면 가운데가 크게 비어 있어 레이아웃은 P0 화면 작업에서 다듬어야 한다.

`tsconfig.json`의 `include`에서 `.expo/types`와 `expo-env.d.ts`가 빠졌다. 내가 지운 게 아니라 `expo start`가 스스로 갱신했다("TypeScript: The tsconfig.json#include property has been updated"). `tsc --noEmit`은 그대로 통과한다.

## 11. P0 화면 1차 (2026-09-21)

"남의 경조사에 돈을 주고 그 사람 원장에서 확인한다"는 핵심 루프를 끝까지 이었다. 행사 축·받은돈 연속 입력·홈 본체·계정은 2차로 남겼다.

### 11.1 사용자 결정 4~10번의 성격
docs/02 §8.2에 따로 적었다. **CTO가 "모두 추천안대로 갈까요"라고 묶어 물었고 사용자가 "응 진행하자"로 답한 것**이며 개별 항목을 따로 언급하지 않았다. 나중에 뒤집을 때 "내가 고른 게 아니라 추천안을 통째로 받은 것"임을 알아볼 수 있도록 표에 뒤집기 비용까지 적어 두었다. 그중 5번(공동 부조를 두 사람 모두 전액)이 가장 비싸다. 기존 기록의 수지 해석이 통째로 달라진다.

### 11.2 화면을 눈으로 확인한 방법
- 시뮬레이터에 **탭을 보낼 수단이 없다.** osascript로 창을 조작하려면 접근성 권한이 필요한데 비대화형 세션에서는 켤 수 없다. Xcode 27은 Simulator.app 대신 DeviceHub를 쓴다.
- 그래서 두 가지로 우회했다. (1) `EXPO_PUBLIC_DEV_SIGNIN=auto` 일 때 로그인 화면이 스스로 개발 로그인을 실행한다. (2) 화면 이동은 `xcrun simctl openurl <device> "exp://127.0.0.1:8081/--/<경로>"` 딥링크로 한다. expo-router의 그룹 `(app)`·`(tabs)`는 URL에 안 나오므로 `/people`·`/ledger`·`/person/<id>` 그대로 쓴다.
- 캡처는 `xcrun simctl io <device> screenshot <파일>`. 9종을 찍어 데이터가 있는 상태와 빈 상태를 둘 다 봤다. Metro 로그의 경고·오류는 0건.
- **주의** — `expo start`가 이미 돌고 있어도 `.env` 변경은 재시작해야 반영된다. 파일을 고친 뒤 앱을 `terminate` → `openurl`로 다시 띄워야 새 번들을 본다. 처음에 이걸 몰라 옛 화면을 찍고 잘못 판단할 뻔했다.

### 11.3 개발 전용 로그인 진입점
- `src/auth/devSignIn.ts`. `__DEV__` **그리고** `EXPO_PUBLIC_DEV_SIGNIN`이 `1`(버튼) 또는 `auto`(자동)일 때만 산다. 조건을 컴포넌트 밖 모듈 상수로 올려 번들러가 죽은 가지를 지울 수 있게 했다.
- **실제로 확인했다.** `npx expo export`한 번들에서 개발 계정 메일·비밀번호·버튼 문구가 전부 0건이다. anon 키와 프로젝트 URL만 인라인된다(설계상 정상). `.env.local`은 gitignore가 덮는다.
- 테스트 계정 `dev-sim@ppurin-test.kr`은 작업이 끝나고 지웠다. 프로젝트에 남은 장부 0권을 확인했다.
- 콘솔 등록이 끝나면 이 진입점을 지울지 결정한다(checklist 5b).

### 11.4 QA가 잡은 것과 수정
- **P0 — 사람 상세(뷰)와 사람 편집(테이블)이 같은 쿼리 키를 썼다.** `person_balances`에는 `phone`·`memo`가 없어서, 상세를 본 뒤 30초 안에 편집에 들어가면 캐시의 뷰 행을 그대로 받아 두 필드가 빈 문자열이 되고 **저장하면 전화번호와 메모가 영구 삭제**됐다. 반대로 편집 후 상세로 돌아오면 수지가 0원으로 보였다. QA가 QueryObserver로 재현했다. `people.detail`(테이블)과 `people.balance`(뷰)로 키를 갈랐고, 편집 폼은 서버 값으로 **한 번만** 채우도록 ref 가드를 뒀다. 시뮬레이터에서 상세 → 편집 순서로 전화번호·메모가 살아 있는 것을 확인했다.
- **중간 실패가 고아와 중복을 만들었다.** 사람 생성은 성공했는데 행사 생성이 실패하면 사람만 남고, 다시 저장을 누르면 같은 이름을 또 만들었다. 이번 저장에서 만든 것을 `useRef`에 기억해 재시도가 이어서 진행하게 했다. 실행 취소도 이 ref를 보고 정확히 지운다. **S09 받은돈 연속 입력도 같은 다단 저장 구조라 이 패턴을 지금 정해 둔 것이 중요하다.**
- 장부를 전환해도 S17의 이름 입력칸과 발급된 초대 코드가 옛 장부 것으로 남아, **B 장부에 A 이름을 덮어쓸 수 있었다.** `currentLedgerId`에 묶어 따라가게 했다.
- 저장 버튼 이중 탭 창 — `findMatchingEvent` 왕복 동안 버튼이 열려 있어 기록이 두 건 생길 수 있었다. `checking` 플래그로 덮었다.
- PRD §3.2의 "더 입력"에서 **장소와 공동 부조자가 빠져 있었다.** `coPersonId`는 draft와 검증에만 있고 UI가 없어 죽은 코드였다. 둘 다 넣었다.
- 그 밖에 — 토스트에 행사 제목 추가, 기존 행사 조회 실패를 삼키지 않기, "새 사람으로 추가"를 동명이인일 때도 보이기, 칩 터치 영역 44pt, 자동완성 prefix 와일드카드 이스케이프, `findMatchingEvent` 후보 상한 3 → 10.
- **`networkMode`를 `'online'` → `'always'`로 바꿨다.** `onlineManager`를 NetInfo에 연결하는 순간 오프라인 저장이 조용히 큐에 쌓였다가 자동 재전송된다. 그것이 docs/03 결정 13이 "만들지 않는다"고 못박은 오프라인 쓰기 큐다. 코드 옆에 연결하지 말라고 적어 두었다.

### 11.5 이번 차수에서 배운 검증 공백
통합 검증 78건이 리포지토리 단위만 보고 있어서 **S02의 3단 순차 저장과 실행 취소가 한 번도 검증되지 않았다.** 중간 실패 결함이 그래서 안 잡혔다. 빠른 기록 흐름(사람 → 행사 → 기록)과 실행 취소 두 경우(새 사람 / 기존 사람+새 행사)를 검증에 넣어 87건이 됐다. 화면이 하는 일을 화면 밖에서 재현할 수 있게 도메인 함수(`undoPlan`)로 빼 둔 덕분에 가능했다.

### 11.6 기획 대비 바꾼 것
- **자동완성 행의 "마지막 기록 요약"을 누적 수지 요약으로 대신했다.** PRD §3.2는 "2024.05 결혼식 10만원 받음"을 요구하지만 `person_balances` 뷰에 마지막 기록의 상세가 없다. 행마다 따로 조회하면 N+1이 된다. 대신 "준 10만원 · 받은 5만원"을 보여 준다. 동명이인을 구별한다는 목적은 같게 달성한다. 뷰에 마지막 기록 컬럼을 더하는 것은 2차에서 판단한다.
- **"새 사람으로 추가"를 일치가 있어도 보여 준다.** PRD는 "일치가 없으면 첫 행에"라고만 적었는데, 그대로 두면 이름이 똑같은 진짜 다른 사람을 빠른 기록에서 만들 수 없다. docs/02 §5가 동명이인을 허용하므로 일치가 없으면 첫 행, 있으면 마지막 행에 항상 둔다.
- **장소는 새 행사를 만들 때만 저장한다.** `place`는 `events`의 컬럼이라 기존 행사에 기록을 붙일 때는 쓸 곳이 없다. 입력칸에 그 사실을 적었다.

## 11b. P0 화면 1차 — 코디네이터 검토 기록 (2026-09-21)

사용자가 질문 4~10을 일괄 추천안으로 확정했고(개별 검토가 아니라 "응 진행하자" 한 마디였다. 뒤집고 싶으면 docs/02 §8.1을 보라) 핵심 루프 하나를 끝까지 만들었다. 탭 셸, 장부(S17), 사람 목록·상세·편집·동명이인(S03·S04·S05·S15), 준돈 빠른 기록(S02)이다.

### 11.1 세션이 죽었는데 "장부가 없습니다"로 보이던 문제

내가 화면을 직접 확인하다 재현했다. 시뮬레이터에 계정 A의 세션이 남은 상태에서 A를 서버에서 지우고 다른 계정으로 앱을 열었더니, **장부가 멀쩡히 있는데도 "장부가 없습니다 — 초대 코드를 입력하세요" 화면에 갇혔다.** 앱 데이터를 지우면 정상이었다.

원인은 **삭제된 계정의 JWT도 만료 전까지는 형식상 유효**하다는 점이다. 그 토큰으로 `ledger_members`를 조회하면 RLS가 오류 없이 0건을 돌려준다. 앱에는 "진짜 장부가 0권"과 구별할 방법이 없어 그대로 `none` 상태가 된다. 토큰 갱신이 실패할 때까지(최대 1시간) 이 화면에 머문다.

현실 경로 — 다른 기기에서 계정을 지웠을 때, 관리자가 계정을 지웠을 때, 토큰이 무효해졌을 때. 앱이 계정 삭제를 정식 기능으로 제공하므로 드물지 않다.

고친 방법 — 장부가 0권으로 보이면 `supabase.auth.getUser()`로 서버에 사용자를 물어보고, 오류면 로그아웃해 로그인 화면으로 돌려보낸다. `getUser()`는 로컬 토큰을 파싱하는 게 아니라 서버에 확인하므로 이 구별이 된다. 로그아웃 시 저장소의 현재 장부 id도 함께 지우게 했다(전에는 메모리만 비웠다).

검증 — 시뮬레이터에서 계정을 서버에서 지운 뒤 앱을 다시 열어 **로그인 화면으로 돌아가는 것을 직접 확인**했다. 고치기 전에는 "장부가 없습니다"에 갇혔다.

**교훈** — RLS가 돌려주는 빈 결과는 "권한이 없다"와 "데이터가 없다"를 구별해 주지 않는다. 빈 결과에 의미를 부여하는 화면(빈 상태, 온보딩 유도)은 세션이 살아 있다는 전제를 따로 확인해야 한다.

### 11.2 부수 확인

- 계정을 지우자 장부·사람 4명·기록 4건이 함께 사라졌다. 0002의 고아 장부 정리 트리거가 실제 프로젝트에서 도는 것을 확인한 셈이다.
- 사람 목록의 수지 표시가 맞다. 준 10만원·받은 15만원 → "50,000원 더 받음", 준 20만원 → "200,000원 더 줌".
- 시뮬레이터에 탭을 보낼 수단이 없다(Xcode 27은 Simulator.app 대신 DeviceHub, 접근성 권한 불가). 화면 확인은 자동 개발 로그인과 딥링크(`exp://<host>:8081/--/<경로>`)로 한다.

## 12. 로그인 수단 변경 — Kakao 제외, 메일·비밀번호 추가 (2026-09-24)

사용자 원문 — "1. 애플 개발자 계정 있어 / 2. 구글로그인, 애플로그인, 일반회원가입 3개만 지원할게(카카오제외)".
확정 결정 12번(Apple+Google+Kakao)을 뒤집는 변경이라 docs/02 §8.1의 12번 행과 §8.2b에 무엇이 왜 바뀌었는지 남겼다.

### 12.1 Kakao를 빼면서 지키기로 한 것
docs/04 §3.1의 Kakao 조사 결과(기본 제공 프로바이더 여부, REST API 키·Client Secret·Redirect URI, 동의 항목, `account_email`의 비즈 앱 제약, 네이티브 SDK 경로의 미검증 항목)를 **지우지 않고 "1단계에서 제외됨(2026-09-24)" 표시만 붙여 보존**했다. 나중에 되돌리기로 하면 재조사 없이 그대로 쓴다. 조사 결과를 버리는 것이 가장 비싼 낭비다.

### 12.2 되살린 것 — 09-16에 명시적으로 배제했던 메일·비밀번호
docs/02 §3.12는 "이메일·비밀번호 로그인, 비밀번호 재설정 흐름을 만들지 않는다"고 적었던 자리다. 되살리면서 네 흐름이 새로 생겼다.
- 회원가입(`app/(auth)/sign-up.tsx`) — 비밀번호 규칙은 8자 이상 + 영문·숫자. 서버 최소는 6자라 앱 규칙이 더 엄격하다. 규칙을 화면에 적고 타이핑 중에 위반을 알린다.
- 메일 확인 대기·재발송(`check-email.tsx`) — **메일 확인은 켠 채로 둔다.** 오타 메일과 남용을 막는 장치다.
- 비밀번호 재설정(`forgot-password.tsx` → 메일 링크 → `reset-password.tsx`).
- 오류 문구 한국어 매핑(`src/auth/errors.ts`) — 잘못된 자격 증명·미확인 메일·이미 가입·약한 비밀번호·이전과 같은 비밀번호·속도 제한·만료 링크·네트워크를 구분한다. 영문 원문은 어떤 경로로도 화면에 나가지 않는다.

### 12.3 실측한 사실 — 내장 SMTP 제한은 "메일이 안 가는" 문제가 아니다
Supabase 내장 메일 발송이 시간당 제한에 걸리면 **`signUp` API 자체가 429 `over_email_send_rate_limit`로 거부된다.** 메일만 못 가는 게 아니라 **신규 가입이 통째로 막힌다.** 실제로 이번 작업 중 제한에 걸려 가입 검증을 못 돌렸다.
- 그래서 checklist 7단계에 **⛔ 출시 차단 항목**으로 커스텀 SMTP를 올렸다.
- 통합 검증도 이에 맞춰 나눴다. 메일을 보내지 않는 경로(관리자 API로 만든 계정의 로그인·비밀번호 변경·미확인 거부·틀린 비밀번호)는 **항상** 검증하고, 메일을 보내는 경로(가입·재발송·재설정 요청)는 제한에 걸리면 SKIP을 찍는다. 검증이 환경 탓에 빨갛게 되는 것과 진짜 실패를 구분하기 위해서다.

### 12.4 비밀번호 로그인이 생기면서 늘어난 위험
- **무차별 대입.** Supabase 기본 속도 제한에 기댄다. 앱도 연속 3회 실패부터 "비밀번호를 잊었다면 재설정을 이용해 주세요"를 띄운다. 서버 쪽 Rate Limits 현재 값을 확인해 적어 두는 항목을 checklist 2단계에 넣었다.
- **서버 최소 길이가 앱보다 느슨하다.** 앱을 거치지 않는 경로(관리자 API 등)에서 6자 비밀번호가 만들어질 수 있다. 대시보드에서 최소 길이를 8자로 올리는 항목을 checklist에 넣었다.
- **재설정 링크 자체가 계정 접근 권한이다.** 링크로 세션이 생긴 뒤 앱을 강제 종료하면 비밀번호를 바꾸지 않고도 다음 실행에서 홈으로 간다(`resetPending`이 메모리에만 있기 때문). Supabase 고유 성질이라 이번 범위에서 막지 않았지만 알고는 있어야 한다.

### 12.5 개발 전용 로그인 삭제
`src/auth/devSignIn.ts`와 호출부, `.env.local`·`.env.example`의 세 키를 지웠다. 정식 메일 로그인이 생겨 존재 이유가 사라졌다. 번들에 관련 문자열이 0건인 것을 확인했다.

### 12.6 QA가 찾은 P0 — 정적 검증 5종이 전부 통과한 상태에서 나왔다
**인증 딥링크에 대응하는 라우트가 없어 expo-router가 "Unmatched Route"로 보냈고, 사용자가 거기서 빠져나올 수 없었다.** `tsc`·단위 테스트·로컬 SQL·원격 통합·번들 export가 전부 통과한 상태였다. **시뮬레이터에서 실제로 링크를 눌러 봐야만 잡히는 결함이었다.**
- 고친 방법 — `app/+native-intent.ts`가 `/auth/*` 경로를 라우터에서 가로채 로그인 화면으로 보내고, 코드 교환은 `AuthProvider`가 원본 URL로 따로 한다. 안전망으로 `app/+not-found.tsx`가 `/`로 되돌린다.
- 함께 고친 것 — 새 비밀번호 화면의 탈출 버튼(만료 링크로 들어오면 막다른 골목이었다), 딥링크 중복 처리 가드(같은 코드를 두 번 교환하면 `resetPending`이 내려가 **비밀번호를 안 바꾸고도 홈으로 샜다**), `alive` 가드가 `resetPending`을 영구 true로 고정하던 문제, OAuth 경로의 예외 처리(버튼이 영구 로딩으로 잠겼다), 비밀번호 길이를 글자가 아닌 바이트로(한글이 섞이면 bcrypt가 조용히 뒤를 자른다).

**교훈** — 딥링크로 들어오는 화면은 "라우트가 존재하는가"를 별도로 확인해야 한다. 코드가 맞아도 라우터가 다른 화면을 띄우면 기능이 없는 것과 같다. 그리고 이런 부류는 정적 검증을 아무리 늘려도 안 잡힌다.

## 13. P0 화면 2차 (2026-09-24)

S08 행사 생성·편집, S06 행사 목록, S07 행사 상세, S09 받은돈 연속 입력, S10 기록 상세·편집, S01 홈 본체, S16 계정을 만들었다. P1(검색·통계·답례 전용 화면·연락처·내보내기)은 범위 밖이다.

### 13.1 도메인을 먼저 뽑아 두고 화면을 얹었다
`src/domain/event.ts`(행사 검증·연도 묶기·내 행사 잠금), `receiving.ts`(연속 입력 초안·이어받기·측 전환), `account.ts`(장부별 삭제 결과)를 순수 함수로 만들고 `node --test`로 덮었다. 화면은 이 함수를 부르기만 한다. DB CHECK와 같은 규칙을 앱에서도 한 번 더 검사하는 이유는 서버 오류 문구가 영문이라 그대로 못 쓰기 때문이다.

### 13.2 시뮬레이터에서 로그인 상태를 만드는 법
탭·키 입력이 없는 환경이라 로그인 화면을 통과할 수 없었다. Expo Go의 AsyncStorage(`.../RCTAsyncLocalStorage/manifest.json`)에 진짜 Supabase 세션을 직접 써 넣어 해결했다. 값이 1024자를 넘으면 `md5(key)` 이름의 파일로 나가고 manifest에는 `null`이 들어간다. 화면 이동은 `xcrun simctl openurl`의 `exp://127.0.0.1:8081/--/<path>`로 한다.

### 13.3 QA가 찾은 것 — 정적 검증 5종을 통과한 상태에서 나왔다
1차와 같은 교훈이 반복됐다. `tsc`·단위·로컬 SQL·원격 통합·번들 export가 전부 통과한 코드에서 아래가 나왔다.

- **S10의 측 편집이 영영 렌더되지 않았다.** `getEntry`가 붙여 오는 행사 컬럼에 `side_a_label`이 없는데 화면이 `as { side_a_label?: string | null }`로 단언해 타입 검사를 우회했다. **단언은 타입 검사를 끄는 스위치다.** 조인 컬럼을 늘리고 단언을 없앴다.
- **S07 명부가 100건에서 조용히 잘렸다.** 같은 라운드의 S06은 `useInfiniteQuery`로 이어받는데 정작 330명이 모이는 S07만 단일 `useQuery`였다. 목록 헤더 건수도 받아 온 페이지 수가 아니라 집계를 쓰도록 바꿨다.
- **조회 실패를 "데이터 없음"으로 읽는 곳이 네 군데 더 있었다.** 1차에서 배운 교훈을 홈에만 적용하고 나머지 화면에 옮기지 않았다. `src/ui/LoadFailed.tsx`로 뽑아 홈·S06·S07·S09·S10에서 같이 쓴다. **교훈을 한 화면에만 적용하면 교훈이 아니다.**
- **S09에서 저장이 중간에 실패한 뒤 이름을 고치면 앞 사람에게 돈이 붙었다.** 중복 생성을 막으려고 둔 `created` ref가 이름을 기억하지 않아 생긴 오귀속이다. 이름을 함께 기억해 같은 이름일 때만 재사용한다. 같은 구조가 S02(`record.tsx`)에도 있어 함께 고쳤다.
- **파괴적 확인 문구가 실제로 일어날 일과 달랐다.** S07 삭제는 집계가 도착하기 전에 누르면 "기록 0건"으로 단정했고, S16은 최대 30초 묵은 구성원 수로 "나만 빠진다"고 안내할 수 있었다. 전자는 "확인하지 못했습니다"로 보수적으로 적고, 후자는 다이얼로그를 띄우기 직전에 구성원 수를 다시 받는다. **되돌릴 수 없는 조작의 확인 문구는 캐시가 아니라 방금 받은 값으로 만든다.**
- **0행이 갱신돼도 "저장됨"으로 화면을 닫았다.** PostgREST는 대상 행이 없어도 오류가 아니라 빈 배열을 준다. `updateEntry`·`updateEvent`가 빈 결과를 오류로 바꾼다.

### 13.4 계정 삭제의 반대 방향 부분 실패
기존 주석은 "RPC가 실패하면 계정을 남기므로 데이터가 주인 없이 떠도는 방향으로는 실패하지 않는다"고 적었지만, 보호하던 것은 고아 데이터뿐이었다. 장부 정리가 커밋된 뒤 `deleteUser`만 실패하면 데이터는 이미 사라졌는데 화면은 "삭제하지 못했습니다"라고 안내한다. Edge Function이 그 경우에 `prepared: true`를 함께 주고, 앱은 "장부 정리는 끝났지만 계정 삭제가 남았습니다"로 구분해 안내한다. **Edge Function 재배포는 권한 정책에 막혀 못 했다.** 앱 코드는 구 응답도 기존과 같은 문구로 처리하므로 회귀는 없지만, 이 구분이 살아나려면 코디네이터가 `supabase functions deploy delete-account`를 한 번 돌려야 한다.

### 13.5 계정 삭제를 실제로 실행해 검증했다
테스트 계정을 만들어 S16이 부르는 것과 같은 요청(사용자 JWT로 `delete-account` 호출)을 보내고, 서버에서 사용자·장부·구성원이 0건이 되는 것을 확인했다. 죽은 세션이 남은 기기에서 앱을 다시 열면 로그인 화면으로 떨어지는 것도 확인했다. 버튼 탭 자체는 시뮬레이터에 입력 수단이 없어 못 눌렀다.

## 14. 구조 변경 — 홈 방향 탭과 통계 탭 (2026-09-24)

**사용자가 실기기에서 직접 써 본 뒤 요청한 변경이다.** 원문은 "홈에 준돈·받은돈 2개 상단 탭", "하단 기록 버튼 너무 좋다", "우상단에 검색버튼", "사람탭을 삭제하고 통계탭으로", "행사탭은 무슨 필요인지 잘 모르겠음"이다.

### 14.1 기획 판단을 뒤집었다
docs/02 §4.1은 "통계는 1차 세그먼트가 매일 보는 화면이 아니다"라며 탭에서 뺐다. 실제 사용자는 반대로 통계를 탭에 두기를 원했다. **실사용자가 써 본 결과가 사전 추정보다 우선한다.** 문서에 뒤집은 사실과 이유를 남겼다.

### 14.2 "필요 없어 보이는 화면"을 지우지 않은 이유
사용자는 행사 탭을 "무슨 필요인지 모르겠다"고 했다. 그러나 **받은돈은 행사에 속해야만 기록된다**(스키마상 `entries.event_id`가 NOT NULL이고 방향은 `events.is_mine`에서 파생된다). 행사 화면을 지우면 내 행사 명부 입력(S09)으로 가는 길이 통째로 끊긴다. 사람 화면도 병합·삭제 같은 관리 동작이 거기에만 있다.
- **탭에서는 빼되 화면은 더보기로 옮겼다.** 일상 동선에서 치우는 것과 기능을 없애는 것은 다르다.
- 대신 일상 동선을 새로 깔았다. 사람은 홈 목록의 이름을 눌러, 행사는 받은돈 탭 상단의 줄과 더보기에서 들어간다.

### 14.3 PostgREST에서 조인 컬럼으로 부모 행을 정렬하는 법 (실측)
준돈·받은돈 목록은 행사 날짜 내림차순이어야 하는데, `entries`에는 날짜가 없고 `events.date`를 봐야 한다.
- `.order('date', { referencedTable: 'events' })` — **틀렸다.** 임베드된 쪽만 정렬하고 부모 순서는 그대로다. 오류도 나지 않고 날짜가 뒤섞인 결과가 조용히 돌아온다.
- `.order('event(date)', { ascending: false })` — **맞다.** 임베드 별칭(`event:events!inner(...)`)을 그대로 쓴다. 별칭이 아니라 테이블 이름(`events(date)`)을 쓰면 "not an embedded resource" 오류가 난다.
- 방향 필터(`.eq('event.is_mine', ...)`)는 **`!inner` 조인이어야** 걸린다. 바깥 조인이면 조건이 무시된다.
- **세 가지 모두 틀려도 서버는 오류를 내지 않는다.** 그래서 통합 검증에 방향별 행 수와 날짜 내림차순을 확인하는 케이스를 넣었다. 실기기 검증만으로는 이 회귀를 못 잡는다.

### 14.4 QA가 찾은 것 — 구조를 바꾸면 기존 화면의 등급이 달라진다
- **사람 원장(S04)이 조회 실패를 "이미 삭제된 사람"으로 표시하고 있었다.** 코드는 그대로인데 **사람 탭이 사라지면서 이 화면이 주 도착지가 되어** 결함의 등급이 올라갔다. 교훈은 이것이다 — **동선을 바꾸면 기존 화면의 결함 등급도 다시 매겨야 한다.** 코드를 안 건드렸으니 괜찮다는 가정이 틀린다.
- **통계 기본 연도를 기기의 올해로 고정한 것이 문제였다.** 2025년에만 기록한 장부를 2026년에 열면 어떤 연도 칩도 선택 표시가 안 된 채 화면 전체가 0으로 보인다. 빈 상태 안내도 안 나온다(행은 있으니까). `defaultYear(years, thisYear)`로 "세그먼트 안에 있는 값"을 보장한다. **세그먼트의 기본 선택은 언제나 세그먼트 안에 있어야 한다.**
- **집계 축이 다른 두 숫자를 나란히 놓으면 문구가 거짓말을 한다.** `stats_by_year`의 `cnt`는 미확정을 포함하고 `total`은 제외한다. "12건 · 미확정 3건 제외"라고 적으면 사용자는 12가 이미 뺀 값이라고 읽는다. "미확정 3건은 합계에서 빠짐"으로 무엇에서 빠졌는지 적는다.
- 방향별 미확정을 나누지 않아 **준돈 탭에 받은돈의 미확정 건수가 섞여 나왔다.** 시뮬레이터 스크린샷을 보고 먼저 잡았다.

### 14.5 시뮬레이터에서 상단 탭을 검증하는 법
탭 입력이 없어 받은돈 탭을 누를 수 없었다. `DEFAULT_DIRECTION`을 잠시 뒤집어 스크린샷을 찍고 되돌렸다. 딥링크 파라미터를 새로 만드는 것보다 낫다 — 검증만을 위한 제품 표면을 늘리지 않는다.

### 14.6 사고 기록 — 정리 스크립트가 사용자 실계정을 지웠다 (2026-09-24)

**무슨 일이 있었나.** 라운드 끝의 "테스트 계정·데이터 전부 삭제, 사용자 0명·장부 0권 확인"을 `auth.admin.listUsers()`로 **모든 사용자를 훑어 전부 지우는** 일회용 스크립트로 수행해 왔다. 이번 라운드 직전에 사용자가 **실기기에서 자기 계정(`donghan.cocoperry@gmail.com`)으로 앱을 직접 써 봤다.** 그 계정이 목록에 함께 잡혀 테스트 계정과 같이 삭제됐다. 계정과 개인 장부, 그 안의 기록이 모두 사라졌다. **되돌릴 수 없다.**

**왜 일어났나.**
- "사용자 0명"이라는 **확인 조건을 삭제 명령으로 바꿔 썼다.** 0명이어야 한다는 것은 *내가 만든 것이 남지 않았다*는 뜻이지 *테이블을 비우라*는 뜻이 아니다.
- 지금까지는 개발용 프로젝트에 테스트 계정만 있었다. **전제가 바뀐 것(실사용자가 들어온 것)을 스크립트가 모른다.** 직전 라운드 보고에 "사용자가 실기기에서 앱을 직접 써 봤다"는 문장이 있었는데도 정리 절차를 그대로 돌렸다.
- 삭제 대상을 **화이트리스트가 아니라 블랙리스트 없이** 잡았다. 안전한 방향은 "지울 것을 명시"이지 "남길 것을 명시"가 아니다.

**앞으로의 규칙.**
1. **정리 스크립트는 내가 이번 실행에서 만든 계정 id만 지운다.** 생성 시점에 id를 모아 두고 그 목록만 삭제한다. `listUsers()` 전수 삭제는 금지한다.
2. 보조 안전장치로 메일 도메인이 `@ppurin-test.kr`인 계정만 대상으로 삼는다. 그 밖의 계정이 목록에 있으면 **삭제하지 말고 보고한다.**
3. "사용자 0명·장부 0권"은 **삭제 명령이 아니라 확인 결과**다. 0이 아니면 무엇이 남았는지 적어 보고하고, 내가 만들지 않은 것이면 그대로 둔다.
4. 실사용자가 들어온 뒤로는 개발 프로젝트라도 **되돌릴 수 없는 일괄 삭제를 돌리지 않는다.**

**복구 가능성.** 조직 플랜상 PITR·백업 복원 경로를 확인하지 못했다. 사용자가 실기기에서 넣은 데이터는 되살릴 수 없다고 보아야 한다.

## 15. 후속 두 건 — 사람별 연도 집계, 공동 부조자 이름 분리 (2026-09-24)

### 15.1 사람별 연도 집계는 뷰가 아니라 함수다
`person_balances` 뷰는 전체 기간만 센다. 연도를 자르려면 인자가 필요해서 `person_stats_by_year(p_ledger_id, p_year)`를 만들었다(0004). 규칙은 뷰와 같다 — 공동 부조는 양쪽 전액, 미확정은 합계에서 제외, 건수에는 포함. 정렬과 상한(`balance desc`, 5명)은 리포지토리가 `.rpc().order().limit()`으로 서버에 맡긴다. 명부가 큰 장부는 한 해에도 수백 명이라 다 받아올 이유가 없다.

화면 쪽 판단 — 사용자가 "전체 기간이 기본이고 연도별 옵션도"라고 했으므로, 페이지의 연도 세그먼트와 **별도 토글**("전체 기간" / "N년만")을 두었다. 세그먼트를 그대로 따르면 기본이 올해가 되어 요구와 어긋난다. 토글의 연도는 세그먼트에서 고른 값을 쓴다.

### 15.2 이름 조각은 문자열이 아니라 id를 든다
"김철수 (+이영희)"를 문자열로 만들어 놓고 화면이 다시 쪼개는 구조였다면 탭 분리가 불가능했다. `entryRowNames()`가 `{ id, name, co }[]`를 돌려주고, `EntryNames` 컴포넌트가 조각마다 자기 원장으로 보낸다. **표시 문자열은 접근성 라벨처럼 한 줄이 필요할 때만 조각에서 파생한다.** 적용 범위는 홈 목록과 행사 상세다. 명부 입력(S09)의 "방금 넣은 기록"은 입력 중 화면을 떠나게 하면 흐름이 끊겨 표시만 하고 탭은 붙이지 않았다.

### 15.3 생성 타입에 손을 댄 이유
`database.types.ts`는 생성 파일이지만 재생성은 코디네이터가 db push 뒤에 한다. 그 전까지 `tsc`가 깨지지 않도록 같은 모양의 `person_stats_by_year` 항목을 손으로 넣었다. 재생성하면 같은 내용으로 덮이므로 충돌은 없다.

### 15.4 정리는 만든 id만, 도메인을 서버에 다시 물어서
14.6의 사고 뒤 첫 라운드다. 즉석 스크립트도 세션 파일에 남은 사용자 id 하나만 대상으로 하고, 삭제 직전 서버에서 메일을 받아 `@ppurin-test.kr`인지 확인한 뒤 지웠다. 목록을 훑는 코드는 쓰지 않았다.


## 16. 0005 — p_year 기본값 (2026-09-24, 코디네이터)

0004의 `person_stats_by_year(p_ledger_id, p_year)`는 p_year에 기본값이 없었다. 리포지토리는 전체 기간을 뜻할 때 p_year를 **생략**하는데, PostgREST는 생략한 호출을 "그 인자가 없는 함수"로 찾으므로 원격에서는 404가 났을 것이다. 타입 생성기도 p_year를 필수로 내보내 `tsc`가 막혔고, 그때 손으로 고쳐 둔 생성 파일이 재생성되며 드러났다.

0005는 본문을 0004와 글자 하나 바꾸지 않고 시그니처에만 `p_year int default null`을 준다. p_ledger_id에는 여전히 기본값을 주지 않는다. 그건 장부 가드다. 같은 이름의 오버로드가 없어 기본값이 모호성을 만들지 않는다.

**교훈 둘.** (1) "인자를 생략하면 NULL"은 SQL에서는 맞고 PostgREST에서는 틀리다. 앱이 생략할 인자는 서버에 기본값이 있어야 한다. (2) 생성 파일을 손으로 고쳐 통과시킨 검증은 검증이 아니다. 재생성 뒤에 다시 돌려야 한다. 이번에는 `db push` 직후 재생성이 관례라 잡혔다.

CREATE OR REPLACE로 함수를 바꿀 때 본문을 기억에서 다시 쓰지 말고 이전 마이그레이션을 열어 복사하라. 내가 처음 쓴 초안은 반환 타입(`id text`)과 조인 모양이 0004와 달랐고, 그대로 밀었으면 반환 타입 변경으로 push가 거부됐거나 집계가 조용히 달라졌다.

검증 — 로컬 SQL 187건(T9.35~36 신설, 생략 호출이 NULL 호출과 같고 빈 결과가 아님), 원격 스모크 42건, 통합 105건, 단위 165건, `tsc` 0건. 실계정 1명·장부 1권은 건드리지 않았다.

## 17. 기록 입력 단순화 — 다섯 필드 (2026-09-24, 빌드 5 사용 뒤)

사용자 원문 — "이름/날짜/경조사종류/금액/메모 이정도 데이터만 입력하게끔 심플하게 고쳐줘". 실기기로 직접 써 본 뒤의 요청이다.

### 17.1 빼는 작업이지 바꾸는 작업이 아니다
스키마는 손대지 않았다. `method`·`attended`·`side`·`co_person_id`·`place`는 컬럼으로 남고 새 기록은 기본값(`cash`, NULL)으로 저장된다. 필드가 아니라 **동작**인 것 — 자동완성·동명이인·±7일 기존 행사 판정·자동 제목·실행 취소·실패 시 폼 유지·"저장 후 다음"의 직전 값 유지 — 는 전부 그대로다. "심플하게"를 동작까지 빼는 것으로 읽으면 30초 기록이 깨진다.

### 17.2 S10은 보이지 않는 컬럼을 덮어쓰지 않는다
편집 페이로드에 `amount`·`memo`만 넣는다. 화면에 없는 형태·참석·측·답례를 페이로드에 기본값으로 넣으면 기존 데이터가 저장 한 번에 지워진다. **화면에 없는 컬럼은 페이로드에도 없어야 한다.**

### 17.3 뒤집힌 결정을 문서에 남겼다
docs/02 §8.1 질문 7(화환·선물 금액)과 10(참석 필드)은 데이터 차원에서는 유지되지만 입력 UI가 사라졌다. 사용자가 실사용 뒤 뒤집었다는 사실을 비고에 적었다. 결정이 바뀐 이유를 지우면 다음 사람이 같은 논의를 반복한다.

### 17.4 따라 나온 죽은 코드
`allowsMissingAmount`는 형태 입력이 사라지면서 호출처가 없어져 지웠다. 준돈은 "금액을 넣어 주세요" 한 가지로 검증한다(받은돈의 미확정 저장은 그대로). `switchSide`와 측 관련 초안 필드도 지웠다. 측별 정산(S07)과 답례 진행률은 기존 데이터만 보여 주게 되는데, 이는 P1 "답례 체크·메모"와 측 입력 위치 재판단으로 넘겼다.

## 18. 동명이인 구분과 준돈 금액 필수 (2026-09-25, 빌드 6 사용 뒤)

사용자 원문 — "5개 맞음 / 동명이인 구분하는 방법 해결해줘".

### 18.1 사용자가 막힌 지점 — 표시와 입력 둘 다였다
코드를 읽어 보니 `people.label`과 `personSubtitle`(라벨 → 그룹)은 있었지만 **(a) 이름을 크게 보여 주는 자리(홈 목록·원장 제목·행사 상세·통계·S10 헤더)는 `name`만 찍어** 라벨이 있어도 "김철수"가 두 번 보였고, **(b) 다섯 필드 입력(S02)에는 라벨을 넣을 자리가 아예 없어** 새 김철수는 무조건 라벨 없이 생겼다. 기획 §5의 "저장 시점에 한 번 묻기"는 S05 편집에만 있었고 S02에는 없었다. 즉 사용자는 (b)로 라벨 없는 동명이인을 만들고 (a)에서 구별하지 못한 것이다.

### 18.2 이름 표시를 한 함수로 모았다
`displayName()`이 "이름 · 라벨"을 만들고, 이름을 보여 주는 모든 곳이 이것만 쓴다. `entryRowNames()`의 조각도 이 함수를 거친다. **화면이 이름 문자열을 직접 조립하지 않는다.** 통계의 사람별 상위는 RPC 결과에 라벨이 없어 상위 5명의 id로 `person_balances`를 한 번 더 읽어 붙인다(스키마를 바꾸지 않기 위한 선택이다).

### 18.3 구별 줄과 "구분 없음"
`distinguishLine()`은 라벨 → 관계 그룹 → 주고받은 요약 → (라벨이 없으면) 최근 기록 시점 순이다. 라벨·그룹·기록이 전부 같은 두 사람은 데이터로는 구별할 수 없다. 그때는 이름을 지어내지 않고 "구분 없음"을 적어 사용자가 라벨을 붙이게 한다. 단위 테스트는 라벨이 다르면·그룹이 다르면·기록이 다르면 줄이 다르다는 것과, 전부 같으면 "구분 없음"이 붙는다는 것을 고정한다.

### 18.4 구분 칸은 같은 이름이 있을 때만 나타난다
다섯 필드 원칙을 지키려고 S02의 구분 칸은 조건부다. 자동완성 결과 중 정규화된 이름이 같은 사람이 있고 사용자가 "새 사람으로 추가"를 골랐을 때만 `Field` 하나가 나타나고 그때는 필수다. 판정(`sameNameCandidates`)과 검증(`validateQuickRecord`의 `sameNameExists`)은 순수 함수다. S09 명부 입력에는 아직 두지 않았다 — 지시 범위가 S02였고, 라벨 없이 생긴 사람은 사람 목록의 "구분 없음"이 받아 준다.

### 18.5 준돈 금액 필수를 도메인에 두었다
S02는 금액 필수인데 S10 편집이 빈 금액을 허용해 어긋났다. `validateEntryAmount(text, isMine)`으로 통일하고 S10은 행사 `is_mine`으로 분기한다. 받은돈은 명부에 이름만 먼저 적는 경우가 있어 미확정을 유지한다.

### 18.6 QA가 잡은 것 — 판정 근거가 화면 목록뿐이면 구멍이 난다
구분 칸의 필수 여부를 자동완성 결과로만 정하면 검색이 실패했거나 정확 일치가 8건 상한에 밀렸을 때 라벨 없는 동명이인이 조용히 생긴다. 저장 직전 `findByNormalizedName`으로 서버에 한 번 더 묻는다(S05 편집이 이미 쓰던 방식). **화면에 보이는 것으로 판단하고, 되돌릴 수 없는 저장 직전에는 서버로 확인한다.** 같은 맥락으로 칸이 숨겨진 뒤 남아 있던 값은 도메인이 버린다.
구분 칸은 "새 사람으로 추가" 행을 누른 뒤가 아니라 이름을 치는 즉시 보여 준다. 그 행을 누르지 않고도 새 사람으로 저장할 수 있기 때문이다. 기획 ③의 문구를 그에 맞췄다.

## 19. 명부 구분 칸, "구분 없음" 재현, 가져오기·OCR 기획 (2026-09-25, 빌드 7 사용 뒤)

### 19.1 S09에 같은 규칙을 복사하지 않고 공유했다
S02의 라벨 규칙을 `newPersonLabelRule(hasExisting, sameNameExists, labelText)`로 뽑아 두 검증 함수가 같은 것을 부른다. S09는 자동완성 목록 대신 정확 일치 조회(`findByNormalizedName`)로 판정한다 — PersonPicker가 목록을 안에 들고 있어 밖으로 꺼내는 것보다 서버에 한 번 묻는 편이 단순하고, 8건 상한 문제도 없다. 구분 칸은 이름 아래 한 줄이고 `carryOver`가 초안을 비우므로 "저장하고 다음" 뒤 사라진다.

### 19.2 "구분 없음"이 안 붙는다는 결함은 재현되지 않았다
배지 조건은 `duplicateNameKeys`(정규화 키, 공백 무시) + `needsLabel`(라벨 없음)이라 그룹과 무관하다. 시뮬레이터에서 그룹 같음·그룹 다름·끝 공백·가운데 공백 네 쌍 전부 붙었고, 실계정 모양(라벨 없음 1 + 라벨 "회사" 1)도 붙었다. 실계정 people 행은 지시대로 네 컬럼만 읽었고 쓰지 않았다. **코드로는 원인을 찾지 못했다.** 남는 가능성은 (가) 사용자가 본 화면이 사람 목록이 아니라 홈 목록(배지가 없는 것이 설계)이거나, (나) 사용자가 라벨 "회사"를 붙인 뒤 라벨 있는 쪽을 보고 "안 붙는다"고 한 경우(라벨이 있으면 안 붙는 것이 맞다), (다) 빌드 7의 영속 캐시가 라벨 편집 전 목록을 잠시 보여 준 경우다. 추측이라 사용자에게 어느 화면의 어느 행이었는지 물어야 한다.

QA가 한 경로를 더 짚었다. 검색(S03b)·병합 시트는 "구분 없음"을 배지가 아니라 구별 줄 **끝**에 문자열로 붙였는데, 준돈·받은돈 요약과 최근 시점이 앞에 오면 한 줄 잘림(`numberOfLines={1}`)에 묻힌다. 사용자가 홈 우상단 검색으로 봤다면 코드상 안 보이는 것이 맞다. 세 화면 모두 배지로 통일했다. **눈에 띄어야 하는 표시는 줄 끝 문자열이 아니라 독립된 요소로 그린다.**

### 19.3 가져오기와 OCR은 순서가 정해져 있다
OCR 결과는 바로 저장할 수 없다(손글씨 한글 정확도). 그래서 가져오기의 미리보기·수정 화면이 OCR의 전제다. 금액 표기 규칙에서 단위 없는 작은 수를 만원으로 추정하지 않기로 한 것은 100배 오차가 조용히 들어가는 것을 막기 위해서다. 파일과 사진은 기기를 떠나지 않는다는 원칙을 처리방침에 적어야 한다.

